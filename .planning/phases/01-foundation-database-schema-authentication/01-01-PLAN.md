---
phase: 01-foundation-database-schema-authentication
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - playwright.config.ts
  - .gitignore
  - tests/e2e/helpers/supabase-admin.ts
  - tests/e2e/helpers/test-users.ts
  - tests/e2e/schema-connectivity.spec.ts
autonomous: false
requirements: [AUTH-05]

user_setup:
  - service: supabase
    why: "Remote hosted Supabase project already provisioned; two Auth settings live only in the Dashboard and cannot be set by `supabase db push`."
    env_vars:
      - name: SUPABASE_DB_URL
        source: "Supabase Dashboard -> Project Settings -> Database -> Connection string (URI). OPTIONAL — only needed for the secondary `supabase migration list --db-url` confirmation; the primary confirmation uses the service-role key already in .env.local."
    dashboard_config:
      - task: "Confirm Auth rate limiting is enabled (credential-stuffing control for threat T-01-03). Local supabase/config.toml sets sign_in_sign_ups = 30 per 5 min per IP; confirm the remote project is equal or stricter."
        location: "Supabase Dashboard -> Authentication -> Rate Limits"
      - task: "Note the current state of the Email provider's 'Confirm email' toggle. INFORMATIONAL ONLY — the signup Route Handler uses auth.admin.createUser({ email_confirm: true }) so signup works either way (RESEARCH Pitfall 4)."
        location: "Supabase Dashboard -> Authentication -> Providers -> Email"

estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Running `npx playwright test` executes against a live dev server at http://localhost:3000 and reports results within ~30 seconds"
    - "A Playwright spec can create a throwaway patient account and a throwaway doctor account directly against the remote Supabase project, without any admin UI"
    - "Every throwaway account created during a test run is deleted from the remote project when that spec file finishes"
    - "All 10 application tables, the anti-double-booking partial unique index, and both RLS helper functions are confirmed present on the live remote database before any auth code is written"
  artifacts:
    - path: "playwright.config.ts"
      provides: "Playwright runner config: testDir tests/e2e, baseURL http://localhost:3000, webServer npm run dev, workers 1"
      contains: "baseURL"
    - path: "tests/e2e/helpers/supabase-admin.ts"
      provides: "Service-role Supabase client for test setup/teardown"
      exports: ["testAdminClient"]
    - path: "tests/e2e/helpers/test-users.ts"
      provides: "Unique-email generation, test account creation by role, and teardown"
      exports: ["TEST_PASSWORD", "uniqueTestEmail", "createTestUser", "deleteTestUserByEmail", "cleanupTestUsers"]
    - path: "tests/e2e/schema-connectivity.spec.ts"
      provides: "Live confirmation that the already-applied migration is present on the remote database and reachable"
      min_lines: 30
  key_links:
    - from: "playwright.config.ts"
      to: ".env.local"
      via: "loadEnvConfig(process.cwd()) from @next/env, so the test process sees SUPABASE_SERVICE_ROLE_KEY"
      pattern: "loadEnvConfig"
    - from: "tests/e2e/helpers/test-users.ts"
      to: "tests/e2e/helpers/supabase-admin.ts"
      via: "imports testAdminClient to call auth.admin.createUser / auth.admin.deleteUser"
      pattern: "testAdminClient"
    - from: "tests/e2e/schema-connectivity.spec.ts"
      to: "supabase/migrations/20260803230000_initial_schema.sql"
      via: "probes every table and index name declared in the migration through the service-role client"
      pattern: "availability_slots"
---

<objective>
Stand up the phase's validation infrastructure (Wave 0) and prove the already-applied database schema is live on the remote Supabase project, so every subsequent plan in this phase has a real, fast, automated verification path.

Purpose: D-10 makes Playwright the only automated test tool in this project. Nothing else in Phase 1 can carry an honest `<automated>` verify until Playwright, its config, and its account fixtures exist. AUTH-05 additionally has a hard data prerequisite — a doctor login account — that cannot come from the admin UI, because that UI is Phase 2 work.

Output: `@playwright/test` installed and gated by a human package-legitimacy checkpoint, `playwright.config.ts`, two reusable test helpers, and a schema/connectivity confirmation spec.
</objective>

<execution_context>
@C:/Users/mellu/Desktop/full_stack_final_project/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/mellu/Desktop/full_stack_final_project/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-database-schema-authentication/01-CONTEXT.md
@.planning/phases/01-foundation-database-schema-authentication/01-RESEARCH.md
@.planning/phases/01-foundation-database-schema-authentication/01-VALIDATION.md
@.planning/phases/01-foundation-database-schema-authentication/COVERAGE.md
@supabase/migrations/20260803230000_initial_schema.sql
</context>

<interfaces>
<!-- Contracts this plan CREATES. Plans 02-05 consume these directly — no exploration needed. -->

tests/e2e/helpers/supabase-admin.ts:
  export function testAdminClient(): SupabaseClient
    // service-role client, { auth: { autoRefreshToken: false, persistSession: false } }

tests/e2e/helpers/test-users.ts:
  export const TEST_PASSWORD: string                       // "TestPassw0rd!" — >= 6 chars, satisfies minimum_password_length
  export function uniqueTestEmail(prefix: string): string  // `${prefix}-${Date.now()}-${randomUUID().slice(0,8)}@example.com`
  export async function createTestUser(role: "patient" | "doctor" | "admin", opts?: { fullName?: string }): Promise<{ id: string; email: string; password: string }>
  export async function deleteTestUserByEmail(email: string): Promise<void>
  export async function cleanupTestUsers(): Promise<void>  // deletes every account this module created in the current process
</interfaces>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy gate for @playwright/test</name>
  <what-built>Nothing yet — this gate runs BEFORE any package-manager install. RESEARCH.md's Package Legitimacy Audit returned a `[SUS]` verdict for `@playwright/test@1.62.1`, triggered solely by the `too-new` heuristic reacting to a 2026-07-30 point release. The corroborating signals are strong (source repo `github.com/microsoft/playwright`, 51.7M weekly downloads), so a false positive is expected — but policy requires a human to confirm before install, and this checkpoint is never auto-approvable.</what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/@playwright/test
    2. Confirm the "Repository" link points to `github.com/microsoft/playwright`.
    3. Confirm weekly downloads are in the tens of millions (not hundreds or thousands).
    4. Confirm the latest published version is `1.62.1` or a later `1.x` release, and that the publish date is recent rather than the package being newly created (check "Versions" — the package should have years of release history).
    5. Confirm the package is published by the `microsoft` npm org.
  </how-to-verify>
  <resume-signal>Type "approved" to allow `npm install -D @playwright/test`, or describe what looked wrong.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install Playwright and create the runner config plus account fixtures</name>
  <precondition>The Task 1 package-legitimacy checkpoint was approved by a human.</precondition>
  <files>package.json, package-lock.json, playwright.config.ts, .gitignore, tests/e2e/helpers/supabase-admin.ts, tests/e2e/helpers/test-users.ts</files>
  <read_first>
    - `.planning/phases/01-foundation-database-schema-authentication/01-RESEARCH.md` — sections "Standard Stack" (exact version 1.62.1), "Validation Architecture", "Pattern 3: Admin (service-role) client"
    - `.planning/phases/01-foundation-database-schema-authentication/01-VALIDATION.md` — "Test Infrastructure" and "Wave 0 Requirements"
    - `.planning/phases/01-foundation-database-schema-authentication/01-PATTERNS.md` — entry for `playwright.config.ts` + `tests/e2e/*.spec.ts`
    - `package.json` — confirm `next@16.2.12` and `@supabase/supabase-js@^2.112.0` are present before adding anything
    - `supabase/migrations/20260803230000_initial_schema.sql` lines 16-22 — the exact `profiles` column set (`id`, `role`, `full_name`, `email`, `created_at`) the fixture must insert
  </read_first>
  <action>
    Run `npm install -D @playwright/test@1.62.1` (pin the exact version, commit the updated lockfile), then `npx playwright install --with-deps chromium`. Add `test-results/` and `playwright-report/` to `.gitignore`.

    Create `playwright.config.ts` at the repo root. It must call `loadEnvConfig(process.cwd())` imported from `@next/env` at the top of the file, BEFORE `defineConfig`, so the Node test process reads `.env.local` and can see `SUPABASE_SERVICE_ROLE_KEY` (`@next/env` ships as a direct dependency of `next@16.2.12` and is already in node_modules; if it does not resolve, replace the import with an eight-line inline reader that does `readFileSync('.env.local','utf8')`, splits on newlines, skips blank and `#`-prefixed lines, and assigns each `KEY=VALUE` into `process.env` — do not add a dotenv dependency). Export `defineConfig` with: `testDir: './tests/e2e'`, `timeout: 30000`, `fullyParallel: false`, `workers: 1` (every spec writes to one shared remote Postgres — parallel workers make account fixtures race), `retries: 0`, `forbidOnly: !!process.env.CI`, `reporter: 'list'`, `use: { baseURL: 'http://localhost:3000', trace: 'on-first-retry' }`, a single `chromium` project using `devices['Desktop Chrome']`, and `webServer: { command: 'npm run dev', url: 'http://localhost:3000', reuseExistingServer: !process.env.CI, timeout: 120000 }`. Do not add any watch-mode or `--ui` flag anywhere.

    Create `tests/e2e/helpers/supabase-admin.ts` exporting `testAdminClient()`. It builds a client with `createClient` from `@supabase/supabase-js` using `process.env.NEXT_PUBLIC_SUPABASE_URL!` and `process.env.SUPABASE_SERVICE_ROLE_KEY!`, options `{ auth: { autoRefreshToken: false, persistSession: false } }`. Throw a descriptive `Error` naming the missing variable if either env var is undefined, rather than letting the SDK fail opaquely. This file exists only under `tests/` and is never importable from application code.

    Create `tests/e2e/helpers/test-users.ts` implementing the `<interfaces>` contract exactly. `TEST_PASSWORD` is `"TestPassw0rd!"`. `uniqueTestEmail(prefix)` returns `` `${prefix}-${Date.now()}-${randomUUID().slice(0, 8)}@example.com` `` using `randomUUID` from `node:crypto` — the timestamp plus random suffix is this project's chosen collision strategy against the shared remote project (RESEARCH left this open; decided here as: unique addresses AND active teardown, so a failed teardown can never break a later run). `createTestUser(role, opts)` calls `testAdminClient().auth.admin.createUser({ email, password: TEST_PASSWORD, email_confirm: true })`, then inserts the matching `profiles` row with `{ id: created.user.id, role, full_name: opts?.fullName ?? \`Test ${role}\`, email }`, pushes the id onto a module-level registry array, and returns `{ id, email, password }`. For `role: "doctor"` it inserts ONLY the `profiles` row — the `public.doctors` catalog row needs NOT NULL `specialty_id` and `location_id` and belongs to ADMIN-01 in Phase 2; AUTH-05 only requires a login account whose `profiles.role` is `doctor`. `deleteTestUserByEmail(email)` resolves the id via `from('profiles').select('id').eq('email', email).single()` then calls `auth.admin.deleteUser(id)` (the `profiles` row disappears through the existing `on delete cascade` on `profiles.id`). `cleanupTestUsers()` drains the registry, calling `auth.admin.deleteUser` for each id and swallowing individual failures so one dead id cannot abort the rest.
  </action>
  <acceptance_criteria>
    - `node -e "require('@playwright/test')"` exits 0.
    - `package.json` devDependencies contains `"@playwright/test": "1.62.1"` (exact pin, no caret).
    - `npx playwright test --list` exits 0 and prints zero errors.
    - `playwright.config.ts` contains the literals `http://localhost:3000`, `npm run dev`, `tests/e2e`, and `loadEnvConfig` (or the inline `.env.local` reader fallback).
    - `grep -q "workers: 1" playwright.config.ts` succeeds.
    - `! grep -Eq -- '--ui|--watch' playwright.config.ts` succeeds (no watch-mode flags, per VALIDATION.md sign-off).
    - `npx tsc --noEmit` exits 0.
    - `tests/e2e/helpers/test-users.ts` exports all five symbols named in `<interfaces>`; `grep -q "email_confirm: true" tests/e2e/helpers/test-users.ts` succeeds.
    - `.gitignore` contains `test-results` and `playwright-report`.
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test --list &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <done>Playwright 1.62.1 is installed and pinned, `npx playwright test --list` runs clean, and both helper modules type-check and export the documented contract.</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Confirm the applied schema is live on the remote database</name>
  <precondition>`.env.local` exists at the repo root and defines NEXT_PUBLIC_SUPABASE_URL and SUPABASE_SERVICE_ROLE_KEY.</precondition>
  <reversibility rating="one-way">D-04 (blocked_periods merged into availability_slots), D-05 (partial unique index on appointments) and D-08 (gist exclusion constraint) are already deployed and read by every later phase; undoing any of them is a data migration, so this task confirms rather than re-decides them.</reversibility>
  <files>tests/e2e/schema-connectivity.spec.ts</files>
  <read_first>
    - `supabase/migrations/20260803230000_initial_schema.sql` — the full file; it is the source of truth for every table, index, and function name asserted here
    - `.planning/phases/01-foundation-database-schema-authentication/01-CONTEXT.md` — `<code_context>` "Already done (do NOT redo)", which records that this migration was already pushed and confirmed
    - `tests/e2e/helpers/supabase-admin.ts` — created in Task 2, used here
  </read_first>
  <action>
    This migration is ALREADY APPLIED to the live remote project (`supabase migration list` previously showed matching local and remote timestamps). No new migration is authored and `supabase db push` is a no-op safety net, not a schema change. Run `supabase migration list --db-url "$SUPABASE_DB_URL"` if `SUPABASE_DB_URL` is set and record the output in the SUMMARY; if it is unset, skip the CLI call — the spec below is the authoritative, credential-light confirmation and requires nothing beyond the service-role key already in `.env.local`.

    Create `tests/e2e/schema-connectivity.spec.ts` as a Playwright spec with no browser interaction (use `test()` without the `page` fixture). It must assert, through `testAdminClient()`:

    1. Every one of the ten tables responds to `from(<table>).select('*').limit(1)` with `error === null`: `profiles`, `specialties`, `languages`, `locations`, `doctors`, `doctor_languages`, `availability_slots`, `appointments`, `favorites`, `notifications`. Iterate the list so the failing table name appears in the assertion message.
    2. Both `security definer` helper functions exist and are callable: `rpc('is_admin')` returns `error === null` and a boolean, and `rpc('is_doctor_owner', { target_doctor_id: '00000000-0000-0000-0000-000000000000' })` returns `error === null` and `false`.
    3. The anti-double-booking guarantee (D-05) is live: `rpc` is not available for index introspection, so assert it structurally instead — insert nothing, and instead confirm the `availability_slots` CHECK named in the migration by attempting `from('availability_slots').insert({ doctor_id: '00000000-0000-0000-0000-000000000000', start_at: ..., end_at: <earlier than start_at> })` and asserting the call returns a non-null `error` (the FK or the `availability_slots_valid_range` check rejects it). Clean up nothing — the insert must never succeed.
    4. The app itself is reachable: a second `test()` that DOES take `page` navigates to `/` and asserts the response status is below 400, proving the `webServer` block starts the Next.js dev server correctly.

    Register the spec so it runs first alphabetically is not required — Playwright ordering is not relied upon; this spec is independent of every other spec.
  </action>
  <acceptance_criteria>
    - `npx playwright test tests/e2e/schema-connectivity.spec.ts` exits 0.
    - The spec references all ten table names literally; `grep -c "notifications" tests/e2e/schema-connectivity.spec.ts` is at least 1.
    - `grep -q "is_doctor_owner" tests/e2e/schema-connectivity.spec.ts` succeeds.
    - The spec creates no rows: `! grep -Eq 'auth\.admin\.createUser' tests/e2e/schema-connectivity.spec.ts` succeeds.
    - `supabase/migrations/20260803230000_initial_schema.sql` is byte-identical to its committed state — `git diff --exit-code supabase/migrations/` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test tests/e2e/schema-connectivity.spec.ts &amp;&amp; git diff --exit-code supabase/migrations/</automated>
  </verify>
  <done>A green spec proves all ten tables, both RLS helper functions, and the availability-slot range constraint are live on the remote project, and the migration file is unmodified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> local node_modules | Third-party code executes at install time and at test time with full local privileges |
| test process -> remote Supabase (service role) | The test harness holds a key that bypasses every RLS policy |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-SC | Tampering | `npm install -D @playwright/test` | high | mitigate | Task 1 is a `blocking-human` legitimacy checkpoint that must be approved before the install; version pinned exactly to 1.62.1 and `package-lock.json` committed (Supabase skill: "always pin package versions and commit lockfiles") |
| T-01-09 | Information Disclosure | `tests/e2e/helpers/supabase-admin.ts` | high | mitigate | The service-role client lives only under `tests/`, is never imported by anything under `app/`, `lib/`, or `components/`, and reads `SUPABASE_SERVICE_ROLE_KEY` — a name deliberately without the `NEXT_PUBLIC_` prefix so Next.js cannot bundle it for the browser |
| T-01-10 | Tampering | remote Supabase project data | medium | mitigate | Test accounts use unique `@example.com` addresses plus active teardown via `auth.admin.deleteUser`; `schema-connectivity.spec.ts` performs read-only probes and one insert that is asserted to FAIL, so it can never leave rows behind |
| T-01-03 | Spoofing | Supabase Auth login endpoint | medium | transfer | Brute-force / credential-stuffing protection is Supabase Auth's built-in per-IP rate limiting; `user_setup` requires the developer to confirm the remote project's Auth rate limits are at least as strict as the local `sign_in_sign_ups = 30` per 5 minutes |
</threat_model>

<verification>
- `npx playwright test` runs the whole (currently one-spec) suite green in under 30 seconds.
- `npx tsc --noEmit` exits 0.
- `npm run build` still succeeds — adding a root-level `playwright.config.ts` must not break the Next.js build.
- `git diff --exit-code supabase/migrations/` exits 0: no schema file was touched.
</verification>

<success_criteria>
- `@playwright/test@1.62.1` is an exact-pinned devDependency, approved through a human legitimacy checkpoint, with `package-lock.json` committed.
- `playwright.config.ts` drives a real dev server at `http://localhost:3000` with `workers: 1` and no watch flags.
- `tests/e2e/helpers/test-users.ts` can mint and destroy `patient`, `doctor`, and `admin` accounts against the remote project, unblocking AUTH-05 without any Phase 2 admin UI.
- The ten deployed tables, both RLS helper functions, and the slot range constraint are confirmed live from an automated spec.
</success_criteria>

## Artifacts this phase produces (Plan 01)

| Artifact | Kind | Symbols / keys |
|----------|------|----------------|
| `playwright.config.ts` | config | default export `defineConfig({...})`; keys `testDir`, `timeout`, `fullyParallel`, `workers`, `retries`, `forbidOnly`, `reporter`, `use.baseURL`, `use.trace`, `projects[0].name = "chromium"`, `webServer.command`, `webServer.url`, `webServer.reuseExistingServer`, `webServer.timeout` |
| `tests/e2e/helpers/supabase-admin.ts` | test helper | `testAdminClient()` |
| `tests/e2e/helpers/test-users.ts` | test helper | `TEST_PASSWORD`, `uniqueTestEmail(prefix)`, `createTestUser(role, opts?)`, `deleteTestUserByEmail(email)`, `cleanupTestUsers()` |
| `tests/e2e/schema-connectivity.spec.ts` | spec | tests `"all ten application tables are reachable"`, `"RLS helper functions are callable"`, `"availability_slots rejects an invalid time range"`, `"the dev server responds on /"` |
| `.gitignore` | config | new entries `test-results/`, `playwright-report/` |
| `package.json` | config | new devDependency `@playwright/test` pinned to `1.62.1` |

<output>
Create `.planning/phases/01-foundation-database-schema-authentication/01-01-SUMMARY.md` when done.
</output>
