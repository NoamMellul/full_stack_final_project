---
phase: 01-foundation-database-schema-authentication
plan: 06
type: execute
wave: 5
depends_on: ["01-04", "01-05"]
files_modified:
  - .planning/phases/01-foundation-database-schema-authentication/01-VALIDATION.md
  - .planning/phases/01-foundation-database-schema-authentication/01-UI-SPEC.md
autonomous: false
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-04, AUTH-05, AUTH-06, AUTH-07]

estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`npx playwright test` runs every requirement spec green in a single pass"
    - "`npm run build`, `npm run lint` and `npx tsc --noEmit` all exit 0 on the finished phase"
    - "Every threat in the phase register that carries a mitigate disposition has an executable assertion behind it, not just a written claim"
    - "The service-role key name appears nowhere outside `lib/supabase/admin.ts` and `tests/e2e/helpers/supabase-admin.ts`"
    - "01-VALIDATION.md's Per-Task Verification Map names the real plan, wave and spec for each of AUTH-01 through AUTH-07, with no TBD rows left"
    - statement: "Every UI Considerations row marked backstop in 01-UI-SPEC.md has been visually confirmed by a human against the running app"
      verification: backstop
  artifacts:
    - path: ".planning/phases/01-foundation-database-schema-authentication/01-VALIDATION.md"
      provides: "Completed verification map with nyquist_compliant and wave_0_complete set true"
      contains: "nyquist_compliant: true"
  key_links:
    - from: ".planning/phases/01-foundation-database-schema-authentication/01-VALIDATION.md"
      to: "tests/e2e"
      via: "each requirement row names the spec file that actually exists on disk"
      pattern: "tests/e2e"
---

<!-- planner-discipline-allow: getSession -->

<objective>
Close Phase 1: prove the whole suite is green together rather than plan by plan, prove every mitigate-disposition threat has an executable assertion behind it, and get a human to confirm the five visual behaviours the UI contract marked as backstop.

Purpose: each plan verified its own slice. Nothing so far has run all seven requirement specs in one pass against the finished application, and backstop rows never pass silently — they need explicit human evidence or they stay unresolved.

Output: a green phase gate, a completed 01-VALIDATION.md verification map, and a signed-off UI contract.
</objective>

<execution_context>
@C:/Users/mellu/Desktop/full_stack_final_project/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/mellu/Desktop/full_stack_final_project/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-database-schema-authentication/01-VALIDATION.md
@.planning/phases/01-foundation-database-schema-authentication/01-UI-SPEC.md
@.planning/phases/01-foundation-database-schema-authentication/COVERAGE.md
@.planning/phases/01-foundation-database-schema-authentication/01-04-SUMMARY.md
@.planning/phases/01-foundation-database-schema-authentication/01-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Whole-phase gate and verification map completion</name>
  <precondition>All of `01-01-SUMMARY.md` through `01-05-SUMMARY.md` exist in the phase directory.</precondition>
  <files>.planning/phases/01-foundation-database-schema-authentication/01-VALIDATION.md</files>
  <read_first>
    - `.planning/phases/01-foundation-database-schema-authentication/01-VALIDATION.md` — the whole file; the Per-Task Verification Map currently has TBD in the Task ID, Plan and Wave columns and every row is pending
    - `.planning/phases/01-foundation-database-schema-authentication/01-01-SUMMARY.md` through `01-05-SUMMARY.md` — for the real task identifiers and spec filenames to write into the map
    - `.planning/phases/01-foundation-database-schema-authentication/COVERAGE.md` — the opt-out list, re-checked against what actually shipped
    - The `<threat_model>` block of each of `01-01-PLAN.md` through `01-05-PLAN.md` — the mitigate rows whose assertions this task re-runs together
  </read_first>
  <action>
    Run the whole gate in one pass and record each command's exit status in the SUMMARY: `npx playwright test` (full suite, no filter), `npx tsc --noEmit`, `npm run build`, and `npm run lint`. If anything fails, fix it here rather than deferring — this is the phase's last automated gate.

    Then run the consolidated security sweep, which re-asserts every mitigate-disposition threat from the five plan registers in one place. Confirm: the root request-interception file exists under the Next.js 16 name and the legacy filename does not exist anywhere; the unrevalidated cookie-read session accessor appears nowhere under `app`, `lib`, `components` or the root gate file; the service-role key name appears only in `lib/supabase/admin.ts` and `tests/e2e/helpers/supabase-admin.ts`; `lib/supabase/admin.ts` still carries its server-only import; the signup Route Handler still writes its role literal inline; no role folder uses Next.js route-group parentheses; and no physical-direction Tailwind utility appears under `app` or `components`. Record the outcome of each check.

    Update `.planning/phases/01-foundation-database-schema-authentication/01-VALIDATION.md`: replace every TBD in the Task ID, Plan and Wave columns of the Per-Task Verification Map with the real plan number, wave number and task name that delivered each requirement (AUTH-01 to Plan 02 Wave 2, AUTH-02 and AUTH-05 to Plan 03 Wave 3, AUTH-03 to Plan 04 Wave 4, AUTH-04, AUTH-06 and AUTH-07 to Plan 05 Wave 4), correct the Automated Command column to the spec filename that actually exists on disk, flip every File Exists cell from the Wave 0 marker to a confirmation, and set every Status cell to green. Add rows for the two specs that exist beyond the original seven — the schema-connectivity spec from Plan 01 and the doctor-login spec — so the map matches the suite. In the frontmatter set `nyquist_compliant: true`, `wave_0_complete: true` and `status: complete`. In the Validation Sign-Off section tick every box that the commands above actually proved, and leave any box unticked whose evidence is missing rather than ticking it optimistically.
  </action>
  <acceptance_criteria>
    - `npx playwright test` exits 0 and reports zero failures across every spec file in `tests/e2e/`.
    - `npx tsc --noEmit`, `npm run build` and `npm run lint` all exit 0.
    - `test -f proxy.ts && test ! -f middleware.ts` succeeds.
    - `! grep -rq "getSession" app lib components proxy.ts` succeeds.
    - `grep -rl "SUPABASE_SERVICE_ROLE_KEY" --include=*.ts --include=*.tsx app lib components tests` lists exactly two paths: `lib/supabase/admin.ts` and `tests/e2e/helpers/supabase-admin.ts`.
    - `grep -q "server-only" lib/supabase/admin.ts` succeeds.
    - `grep -q 'role: "patient"' app/api/auth/signup/route.ts` succeeds.
    - `! grep -rqE '\b(pl-|pr-|ml-|mr-|text-left|text-right)' app components` succeeds.
    - `01-VALIDATION.md` contains no occurrence of the string `TBD`, and its frontmatter contains `nyquist_compliant: true` and `wave_0_complete: true`.
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test &amp;&amp; npx tsc --noEmit &amp;&amp; npm run build &amp;&amp; npm run lint</automated>
  </verify>
  <done>The entire suite plus build, lint and type-check are green in one pass, the security sweep is recorded, and the verification map names real plans, waves and spec files with no TBD left.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The full Phase 1 authentication surface: `/signup`, `/login`, a shared header with a logout control, and three role-guarded home bases at `/patient`, `/doctor` and `/admin`. Everything automatable has already been automated and is green — the five items below are the UI contract's backstop rows, which are visual and cannot be asserted from a Playwright expectation without becoming a brittle screenshot test.</what-built>
  <how-to-verify>
    Start the app with `npm run dev` and open http://localhost:3000/signup.

    1. **Partial-fill retention (signup).** Type a full name and a password, leave Email blank, and submit. Expect: only the Email field shows "Email is required."; the full name and password you typed are still there, unchanged.
    2. **Partial-fill retention (login).** Open http://localhost:3000/login, type an email, leave Password blank, and submit. Expect: only the Password field shows "Password is required."; the email you typed is still there.
    3. **Long-text inside inputs.** On `/signup`, paste roughly 200 characters into Full name. Expect: the text scrolls inside the input; the card does not get wider and nothing overflows the card edge. Repeat with a very long address in the Email field on `/login`.
    4. **Header collapse below 640px.** Sign up as a new patient so you land on `/patient`. Narrow the browser window to under 640px wide. Expect: the logout control becomes icon-only, and the icon's clickable box is at least 44 by 44 pixels (check with the browser inspector).
    5. **Long display name truncation.** Sign up with a full name of roughly 80 characters and view `/patient` at full desktop width. Expect: the name in the header ends in an ellipsis at a fixed width and stays on one line — it must not wrap to a second row or push the logout control off screen.

    Then do one end-to-end pass for confidence: sign up, log out, log back in, refresh the page, and try to open http://localhost:3000/admin as that patient (expect to be bounced to `/`).

    Finally, tick the six Checker Sign-Off boxes in `01-UI-SPEC.md` for the dimensions you confirmed, and set its `status` frontmatter to `approved`.
  </how-to-verify>
  <resume-signal>Type "approved" once all five backstop items and the end-to-end pass look right, or describe exactly which step deviated.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| completed phase -> Phase 2 | Everything shipped here becomes the trusted foundation later phases build on without re-checking |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-20 | Repudiation | phase-level security claims | high | mitigate | Every mitigate row from the five plan registers is re-asserted here as a single consolidated sweep, so a claim that was true in isolation cannot silently regress once all the plans are merged |
| T-01-06 | Information Disclosure | service-role key surface | high | mitigate | A repo-wide search pins the key name to exactly two files, one server-only application module and one test-only helper; any third occurrence fails the gate |
| T-01-07 | Spoofing | root request-interception filename | critical | mitigate | Re-asserted at the phase gate, because the failure mode is silent: the legacy filename produces no build error and no runtime warning, so only an explicit check catches it |
| T-01-21 | Tampering | leftover test accounts on the remote project | low | accept | Test accounts use unique `@example.com` addresses and are torn down per spec; any survivor of a crashed run is an inert unprivileged patient row on a demo project and is cleaned up by the next full-suite pass |
</threat_model>

<verification>
- `npx playwright test` green across every spec.
- `npm run build`, `npm run lint`, `npx tsc --noEmit` all exit 0.
- The consolidated security sweep passes every check listed in Task 1's acceptance criteria.
- A human has confirmed all five backstop rows from `01-UI-SPEC.md`.
</verification>

<success_criteria>
- All four ROADMAP Phase 1 success criteria are demonstrably true: patient signup/login/logout with session persistence, doctor login with admin-created credentials, unauthenticated redirect, and cross-role denial.
- AUTH-01 through AUTH-07 each map to a named, green spec file in `01-VALIDATION.md`.
- `01-VALIDATION.md` has `nyquist_compliant: true` and no TBD rows.
- `01-UI-SPEC.md` is signed off with its backstop rows humanly confirmed.
</success_criteria>

## Artifacts this phase produces (Plan 06)

| Artifact | Kind | Change |
|----------|------|--------|
| `.planning/phases/01-foundation-database-schema-authentication/01-VALIDATION.md` | planning doc | Per-Task Verification Map completed with real plan/wave/spec values; `nyquist_compliant: true`, `wave_0_complete: true`, `status: complete` |
| `.planning/phases/01-foundation-database-schema-authentication/01-UI-SPEC.md` | planning doc | Checker Sign-Off boxes ticked; `status: approved` |

<output>
Create `.planning/phases/01-foundation-database-schema-authentication/01-06-SUMMARY.md` when done.
</output>
