---
phase: 06-dashboards-notifications-localization
plan: 10
type: execute
wave: 6
depends_on:
  - 06-08
  - 06-09
files_modified:
  - tests/e2e/locale-switching.spec.ts
  - components/favorite-toggle.tsx
  - components/notification-bell.tsx
  - components/search/doctor-card.tsx
  - app/patient/favorites/page.tsx
autonomous: true
requirements:
  - I18N-02

estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The interface layout mirrors correctly when Hebrew is selected: no element hugs the wrong edge, and both absolutely-positioned overlays this phase introduced (the favorite heart and the unread badge) sit at the inline-end edge in both directions (I18N-02)."
    - "A repo-wide audit of app/ and components/, excluding generated components/ui/ primitives, finds zero physical-direction Tailwind utilities — the invariant that has held by convention for five phases is now mechanically asserted and is exercised at runtime for the first time."
    - "The notification popover remains fully inside the viewport when Hebrew is active, resolving the UI-SPEC's flagged Base UI Popover RTL anchoring assumption."
    - "No flex-row-reverse utility exists anywhere in app/ or components/ — plain flex rows already reverse under dir=rtl and compensating would double-flip them."
    - "The full Playwright suite is green in both locales, and npm run build succeeds, closing the phase."
    - statement: "The /patient/favorites list scrolls naturally at a large favorited-doctor count with no fixed-height container, confirmed visually in both directions (UI-SPEC overflow backstop)."
      verification: backstop
    - statement: "An unusually long doctor full_name or specialty name on a /patient/favorites row is contained by DoctorCard's existing truncate treatment without breaking the row, confirmed visually in both directions (UI-SPEC long-text backstop)."
      verification: backstop
    - statement: "A failed patient-dashboard appointments fetch and a failed doctor-dashboard count query each degrade to a readable state rather than a fabricated value, confirmed by inducing each failure (UI-SPEC dashboard error backstop)."
      verification: backstop
  artifacts:
    - tests/e2e/locale-switching.spec.ts
  key_links:
    - "The RTL geometry assertions added to tests/e2e/locale-switching.spec.ts are the standing regression guard for I18N-02 — every future phase that adds an absolutely-positioned overlay inherits them."
    - "This plan is the only place the three UI-SPEC backstop considerations and the two flagged RTL assumptions from 06-05 and 06-06 are resolved with evidence."
  prohibitions:
    - "Physical-direction utilities must never be introduced in code this phase authored or modified; only logical-property utilities are permitted."
    - "Never add flex-row-reverse to compensate for RTL — plain flex rows already reverse under dir=rtl and compensating would double-flip them."
    - "An icon that carries no inherent directionality must never be mirrored with a transform; this phase introduces no directional glyph that would need one."
    - "A backstop consideration must never be marked resolved without recorded evidence — if it cannot be confirmed, it stays a backstop and is surfaced at verify time rather than silently passed."
---

<objective>
Close the phase's RTL contract: mechanically audit the whole non-generated component tree for physical-direction utilities, add standing geometry assertions that prove the two new overlays mirror correctly, and resolve with recorded evidence the three UI-SPEC backstop considerations and the two flagged RTL assumptions carried from 06-05 and 06-06.

Purpose: this is the first phase in which `dir="rtl"` is exercised at runtime. Five phases of code have been written under a logical-properties convention that nobody has ever tested in the direction it exists for.
Output: new RTL regression tests, any fixes the audit surfaces, and a recorded resolution for every outstanding backstop and assumption.
</objective>

<execution_context>
@C:/Users/mellu/Desktop/full_stack_final_project/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/mellu/Desktop/full_stack_final_project/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-dashboards-notifications-localization/06-UI-SPEC.md
@.planning/phases/06-dashboards-notifications-localization/06-05-SUMMARY.md
@.planning/phases/06-dashboards-notifications-localization/06-06-SUMMARY.md
@.planning/phases/06-dashboards-notifications-localization/06-08-SUMMARY.md
@.planning/phases/06-dashboards-notifications-localization/06-09-SUMMARY.md
</context>

## Artifacts this phase produces

This plan adds RTL geometry regression tests to `tests/e2e/locale-switching.spec.ts` and applies whatever corrections the audit surfaces in the four files listed in `files_modified`. It creates no new module, route, component or dictionary key.

<tasks>

<task type="auto">
  <name>Task 1: Mechanical logical-property audit and RTL geometry regression tests</name>
  <files>tests/e2e/locale-switching.spec.ts, components/favorite-toggle.tsx, components/notification-bell.tsx, components/search/doctor-card.tsx, app/patient/favorites/page.tsx</files>
  <read_first>
    - components/favorite-toggle.tsx and components/search/doctor-card.tsx (the card-overlay heart positioned at the inline-end corner)
    - components/notification-bell.tsx (the unread badge overlaying the bell trigger at the inline-end corner, and the popover's align and side props)
    - app/patient/favorites/page.tsx (the row-level heart position)
    - components/ui/select.tsx (an example of a generated shadcn primitive carrying Base UI's own side tokens — generated files under components/ui/ are excluded from the audit because those tokens are the library's internal side vocabulary, not authored layout)
    - tests/e2e/locale-switching.spec.ts (the five tests activated in 06-05 — extend this file, do not create a second RTL spec)
    - .planning/phases/06-dashboards-notifications-localization/06-UI-SPEC.md § "RTL mirroring contract (I18N-02)"
  </read_first>
  <action>
    <!-- planner-discipline-allow: pl- pr- ml- mr- text-left text-right left- right- flex-row-reverse -->
    Run a repo-wide audit for physical-direction Tailwind utilities across `app/` and `components/`, excluding the generated `components/ui/` primitives. The prohibited set is the padding pair `pl-` and `pr-`, the margin pair `ml-` and `mr-`, the alignment pair `text-left` and `text-right`, the inset pair `left-` and `right-`, and `flex-row-reverse`. The permitted replacements are the logical equivalents already used throughout this codebase: `ps-`, `pe-`, `ms-`, `me-`, `text-start`, `text-end`, `start-`, `end-`. If the audit finds a match in authored code, replace it with the logical equivalent — never with a direction-specific override and never with a compensating reverse. If it finds none, record that and change nothing.

    `flex-row-reverse` deserves separate mention: a plain flex row already reverses under `dir="rtl"`, so adding a reverse utility would flip it a second time and land it back in the original order. Its absence is a correctness property, not a style preference.

    Then extend `tests/e2e/locale-switching.spec.ts` with geometry assertions that make the mirroring mechanical rather than eyeballed. Add a test titled `"the favorite heart sits at the inline-end edge in both directions"` which loads a search result card in English and records the heart's bounding-box x relative to its card, then repeats with the locale cookie set to Hebrew and asserts the heart has moved to the opposite side of the card. Add a test titled `"the notification badge and popover stay inside the viewport in Hebrew"` which signs in as a patient with at least one unread notification, sets the locale to Hebrew, opens the bell, and asserts the popover's bounding box lies entirely within the viewport width — this is the assertion that resolves the UI-SPEC's flagged assumption that Base UI's `align="end"` mirrors against the ambient `dir` context. If it fails, fix the popover's anchoring with logical props rather than weakening the assertion.

    Both new tests are active `test(` declarations, not placeholders.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/locale-switching.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rnE '(^|[^a-z-])(pl|pr|ml|mr)-[0-9]' app components --include=*.tsx | grep -v 'components/ui/'` returns no match.
    - `grep -rnE 'text-(left|right)|flex-row-reverse' app components --include=*.tsx | grep -v 'components/ui/'` returns no match.
    - `grep -rnE '(^|[^a-z-])(left|right)-[0-9]' app components --include=*.tsx | grep -v 'components/ui/'` returns no match.
    - `tests/e2e/locale-switching.spec.ts` contains the two new test titles and `npx playwright test tests/e2e/locale-switching.spec.ts` reports 7 passed, 0 skipped.
    - The notification popover's bounding box `x` is greater than or equal to 0 and its `x + width` is less than or equal to the viewport width when the locale cookie is `he`.
    - `npx playwright test` (full suite) reports 0 failed.
    - `npm run build` succeeds and `npm run lint` exits 0.
  </acceptance_criteria>
  <done>The logical-property invariant is mechanically asserted rather than assumed, and both overlays this phase introduced have standing regression tests proving they mirror.</done>
</task>

<task type="auto">
  <name>Task 2: Resolve the three UI-SPEC backstop considerations and the two flagged RTL assumptions with recorded evidence</name>
  <files>tests/e2e/locale-switching.spec.ts</files>
  <read_first>
    - .planning/phases/06-dashboards-notifications-localization/06-UI-SPEC.md § "UI Considerations" (the three rows marked backstop: dashboard-level error copy, favorites-list overflow at scale, and favorites-list long-text truncation)
    - .planning/phases/06-dashboards-notifications-localization/06-05-SUMMARY.md (whether `router.refresh()` alone sufficed for the direction change, or the `window.location.reload()` fallback shipped — RESEARCH Assumption A2)
    - .planning/phases/06-dashboards-notifications-localization/06-06-SUMMARY.md (whether the Realtime connection needed an explicit `onAuthStateChange` token refresh — RESEARCH Assumption A1)
    - app/patient/favorites/page.tsx and components/search/doctor-card.tsx (the truncate and max-width treatment the long-text backstop depends on)
    - app/patient/page.tsx and app/doctor/(gated)/page.tsx (the two dashboard error paths the dashboard-error backstop depends on)
    - tests/e2e/helpers/favorites.ts (the fixture helper used to seed a large favorited set and a long doctor name)
  </read_first>
  <action>
    Resolve each outstanding backstop with recorded evidence rather than assertion.

    Favorites-list overflow at scale: seed at least 25 favorites for one test patient through `createTestFavorite`, load `/patient/favorites` in both directions, and confirm the page scrolls naturally with no fixed-height container clipping rows and no horizontal scrollbar. Record the observed row count and whether any clipping occurred.

    Favorites-list long text: seed a doctor whose `full_name` is at least 80 characters, load `/patient/favorites` in both directions, and confirm the row's existing truncate and max-width treatment contains it without pushing the heart off the card or wrapping the row into an unreadable shape. Record the outcome.

    Dashboard error states: induce a failure on each dashboard — for the patient dashboard by making `GET /api/patient/appointments` fail through Playwright's route interception, and for the doctor dashboard by exercising the branch where a count query returns null — and confirm each degrades to a readable state rather than a fabricated number or a blank page. Record what each rendered.

    Convert any of the three into an active regression test in `tests/e2e/locale-switching.spec.ts` where the observation is cheaply automatable; leave the rest recorded as observations. A backstop that could not be confirmed stays a backstop and must be reported as such in the SUMMARY — it must never be quietly marked resolved.

    Finally, record the disposition of both flagged RTL and Realtime assumptions in the SUMMARY: whether `router.refresh()` alone sufficed for the direction change (RESEARCH Assumption A2) and whether the Realtime connection needed an explicit token-refresh listener (RESEARCH Assumption A1), reading each from the earlier plan's SUMMARY rather than re-deriving it.
  </action>
  <verify>
    <automated>npx playwright test</automated>
    <human-check>
      Set the language to Hebrew from the header on `/search`, then walk `/`, `/search`, a doctor profile, `/patient`, `/patient/favorites`, `/patient/appointments`, `/doctor` and `/doctor/schedule`. Confirm on each page that text reads right-to-left, that no control or badge hugs the wrong edge, that the favorite heart and the notification badge both sit at the trailing edge, that the notification dropdown opens fully inside the window, and that no English copy remains outside the `MedRDV` brand name and the `EN` switcher label. Then switch back to English and confirm every page returns to its original left-to-right appearance.
    </human-check>
  </verify>
  <acceptance_criteria>
    - `npx playwright test` (full suite) reports 0 failed.
    - The SUMMARY records, for each of the three UI-SPEC backstop considerations, either the evidence that resolves it or an explicit statement that it remains a backstop.
    - The SUMMARY records the disposition of RESEARCH Assumption A1 and RESEARCH Assumption A2.
    - Seeding 25 favorites and loading `/patient/favorites` produces 25 rendered rows with no horizontal scrollbar on the document body in either direction.
    - Seeding a doctor with an 80-character name produces a `/patient/favorites` row whose card width is unchanged from a short-name row.
    - Intercepting `GET /api/patient/appointments` to fail causes `/patient` to render its error treatment and a Retry control, not a blank section and not a fabricated appointment.
    - `npm run build` succeeds and `npm run lint` exits 0.
  </acceptance_criteria>
  <done>Every backstop consideration and flagged assumption carried by this phase has a recorded disposition, and the phase's full test suite is green in both locales.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rendered layout → user comprehension | A mirrored layout that silently fails leaves a Hebrew reader with controls in unexpected positions, which is a usability and accessibility failure rather than a data one |
| test fixture → shared dev database | This plan seeds an unusually large favorites set and a long-named doctor |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-45 | Tampering | a physical-direction utility surviving into shipped code | medium | mitigate | A mechanical grep audit over `app/` and `components/` runs as an acceptance criterion, and two geometry assertions become standing regression tests |
| T-06-46 | Denial of Service | the popover overflowing the viewport in RTL, making notifications unreachable | medium | mitigate | A bounding-box assertion proves the popover stays inside the viewport width when Hebrew is active; a failure is fixed with logical anchoring props rather than by weakening the assertion |
| T-06-47 | Tampering | bulk fixture rows left in the shared dev database | medium | mitigate | The 25-favorite and long-name fixtures register their ids with `cleanupTestFavorites` and `cleanupTestUsers`, run in the spec's `test.afterAll` |
| T-06-48 | Repudiation | a backstop silently marked resolved without evidence | medium | mitigate | Each backstop's disposition must be recorded in the SUMMARY; an unconfirmed backstop stays a backstop and is surfaced at verify time rather than passed |
| T-06-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package-manager install occurs in this plan |
</threat_model>

<verification>
- `npx playwright test` reports 0 failed across all 33 spec files.
- The three grep audits over `app/` and `components/` (excluding `components/ui/`) return no match.
- `npm run build` succeeds and `npm run lint` exits 0.
- The human RTL walkthrough is confirmed, or its findings are recorded as open items.
</verification>

<success_criteria>
I18N-02 is satisfied with mechanical evidence rather than convention: the logical-property invariant is asserted repo-wide, both new overlays are proven to mirror, the notification dropdown is proven reachable in Hebrew, and every backstop and flagged assumption this phase carried has a recorded disposition.
</success_criteria>

<output>
Create `.planning/phases/06-dashboards-notifications-localization/06-10-SUMMARY.md` when done.
</output>
