---
phase: 06-dashboards-notifications-localization
plan: 09
type: execute
wave: 5
depends_on:
  - 06-07
files_modified:
  - components/site-header.tsx
  - components/logout-button.tsx
  - components/favorite-toggle.tsx
  - components/notification-bell.tsx
  - lib/appointments.ts
  - app/patient/page.tsx
  - app/patient/appointments/page.tsx
  - app/doctor/(gated)/appointments/page.tsx
  - app/patient/favorites/page.tsx
  - app/doctor/(gated)/page.tsx
  - app/doctor/(gated)/schedule/page.tsx
  - app/doctor/change-password/page.tsx
autonomous: true
requirements:
  - I18N-01

estimate:
  tokens: 105000
  raw_tokens: 105000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every patient page, every doctor page, and every shared component outside app/admin and components/admin renders entirely in Hebrew when the locale cookie is he and entirely in English when it is en (I18N-01, D-04)."
    - "With the locale set to English, every retrofitted page renders byte-identical text to what it rendered before this plan — proven by the existing Phase 1-5 Playwright specs staying green without a single assertion being edited."
    - "appointmentBadge() returns a dictionary key instead of a literal English label, and every call site resolves it through t() rather than comparing against an English string."
    - "The reschedule and cancel eligibility predicate on both appointment pages compares against the appointment-status dictionary key, not against the literal text 'Confirmed' — so eligibility is unaffected by the active locale."
    - "Admin pages remain untranslated and English-only: no file under app/admin or components/admin is modified by this plan (D-04)."
    - "No key is added to either dictionary file by this plan: every t() call resolves against the inventory authored in 06-07, enforced by the TranslationKey type."
    - "The language switcher's own labels remain hard-coded literals and are not touched by this retrofit."
  artifacts:
    - lib/appointments.ts
    - components/site-header.tsx
    - app/patient/appointments/page.tsx
    - app/doctor/(gated)/schedule/page.tsx
  key_links:
    - "lib/appointments.ts remains the single source of the derived status label and the upcoming/past split for both appointment pages and the patient dashboard — the label just becomes a key instead of a sentence."
    - "components/site-header.tsx renders through getT() as a Server Component while the switcher, bell, logout button and favorite toggle it hosts each render through useT() as Client Components."
    - "Every t() call site is type-checked against the TranslationKey union derived from dictionaries/en.json."
  prohibitions:
    - "This plan must never add, rename or remove a key in dictionaries/en.json or dictionaries/he.json — the inventory is owned by 06-07 and this plan is a pure consumer."
    - "No file under app/admin/ or components/admin/ may be modified — admin stays English-only per D-04."
    - "Eligibility, permission, or status logic must never be decided by comparing a rendered, translatable string; every such comparison must key off a stable identifier."
    - "Physical-direction utilities must never be introduced in these files; only logical-property utilities are permitted."
    - "No existing Playwright assertion may be edited to accommodate a changed string — if a spec goes red, the retrofit changed the English rendering and the retrofit is wrong, not the spec."
    - "No dashboard, favorites, appointments or notification surface may begin rendering medical content as a side effect of this retrofit."
---

<objective>
Retrofit the authenticated surfaces and the shared components to render through `t()`: the site header and its controls, both appointment pages, both dashboards, the favorites page, the doctor schedule page, the forced password-change page, and the derived appointment-status labels they all share.

Purpose: D-04's translation scope is patient pages, doctor pages and public pages retroactively across Phases 1-5. 06-08 covers the public half; this plan covers the authenticated half and the shared components both halves render.
Output: twelve modified files, zero behavioural change in English, and one function signature change that removes a translatable string from a permission predicate.
</objective>

<execution_context>
@C:/Users/mellu/Desktop/full_stack_final_project/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/mellu/Desktop/full_stack_final_project/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-dashboards-notifications-localization/06-CONTEXT.md
@.planning/phases/06-dashboards-notifications-localization/06-05-SUMMARY.md
@.planning/phases/06-dashboards-notifications-localization/06-07-SUMMARY.md
</context>

## Artifacts this phase produces

This plan changes one public signature: `appointmentBadge()` in `lib/appointments.ts` returns `{ labelKey: TranslationKey; variant }` in place of `{ label: string; variant }`. Everything else is a call-site substitution of a literal for a `t()` lookup across twelve files.

<tasks>

<task type="auto">
  <name>Task 1: Site header and its four hosted controls</name>
  <files>components/site-header.tsx, components/logout-button.tsx, components/favorite-toggle.tsx, components/notification-bell.tsx</files>
  <read_first>
    - components/site-header.tsx (as left by 06-05 and 06-06: no anonymous early return, profiles selected as `full_name, role`, hosting the switcher, the bell and the logout button — it is an async Server Component, so it translates through getT())
    - components/logout-button.tsx (the `Log out` label and the `Could not log out. Please try again.` failure message)
    - components/favorite-toggle.tsx (built in 06-02 — the two aria-labels and the toggle failure message)
    - components/notification-bell.tsx (built in 06-06 — already renders through useT(); audit it for any literal that escaped, such as a Retry label)
    - components/language-switcher.tsx (read only: its `EN`, `עב`, `English` and `עברית` literals are deliberately untranslated and must stay that way)
    - dictionaries/en.json (the `header.*`, `common.*`, `favorite_toggle.*` and `notifications.*` keys authored in 06-05 and 06-07)
    - lib/i18n/server.ts (`getT`) and lib/i18n/locale-provider.tsx (`useT`)
    - tests/e2e/auth-logout.spec.ts (the assertions that must keep passing unedited)
  </read_first>
  <action>
    Replace every user-facing literal in the four files with a `t("<key>")` call. `components/site-header.tsx` is an async Server Component, so it awaits `getT()` from `@/lib/i18n/server`; the other three are Client Components and call `useT()` from `@/lib/i18n/locale-provider`.

    In `components/logout-button.tsx`, translate both the visible label and the failure message. In `components/favorite-toggle.tsx`, translate both aria-labels and the failure message. In `components/notification-bell.tsx`, audit for any literal that escaped 06-06 — most notably the Retry button label, which resolves to the shared `common.retry` key.

    Do not touch `components/language-switcher.tsx`. Its four literals are deliberately hard-coded so a reader of either language can always find their own option; routing them through `t()` would strand a Hebrew reader in an English UI with no findable switch.

    The `MedRDV` brand link stays a literal — it is a proper noun, not copy.

    Do not change the header's role gate, the bell's mount condition, the toggle's optimistic-update behaviour, or the logout button's fetch-then-refresh flow. Only the strings change.

    Use only logical-property utilities in any markup you touch.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/auth-logout.spec.ts tests/e2e/patient-favorites.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test tests/e2e/auth-logout.spec.ts tests/e2e/patient-favorites.spec.ts` reports 0 failed, with `git diff --stat tests/e2e/` reporting no change.
    - With the locale cookie set to `he`, the header on `/patient` renders a Hebrew log-out label and the favorite toggle on `/search` exposes a Hebrew `aria-label`.
    - `git diff --stat components/language-switcher.tsx` reports no change.
    - `components/site-header.tsx` contains `getT` and contains no `useT`.
    - `components/logout-button.tsx`, `components/favorite-toggle.tsx` and `components/notification-bell.tsx` each contain `useT` and contain no remaining user-facing English sentence literal.
    - `git diff --stat dictionaries/` reports no change.
    - `npx tsc --noEmit` exits 0 and `npm run lint` exits 0.
  </acceptance_criteria>
  <done>Every string the header renders on every page of the application, in both roles and logged out, resolves from the dictionary — except the switcher's own self-labelling controls.</done>
</task>

<task type="auto">
  <name>Task 2: Appointment status labels become dictionary keys, across all three call sites</name>
  <files>lib/appointments.ts, app/patient/appointments/page.tsx, app/doctor/(gated)/appointments/page.tsx, app/patient/page.tsx</files>
  <read_first>
    - lib/appointments.ts (the `AppointmentBadge` type and `appointmentBadge()`'s four literal labels; note the header comment explaining that the past label is UI-computed and never written back to the database)
    - app/patient/appointments/page.tsx (its `canManage` predicate compares the badge label against a literal English string — that comparison must stop keying off rendered text; plus every heading, section title, empty state, error state, dialog title and body, button label and in-flight label, and the composed aria-labels on the reschedule and cancel buttons)
    - app/doctor/(gated)/appointments/page.tsx (the same eligibility predicate, deliberately mirrored from the patient page by plan 05-05, and the same categories of copy)
    - app/patient/page.tsx (the dashboard built in 06-03 — it renders `appointmentBadge()` output and its own headings, empty state and quick-link labels)
    - dictionaries/en.json (`appointment_status.*`, `patient_appointments.*`, `doctor_appointments.*`, `patient_dashboard.*`)
    - lib/i18n/dictionaries.ts (`TranslationKey`) and lib/i18n/locale-provider.tsx (`useT`)
    - tests/e2e/appointment-history.spec.ts, tests/e2e/appointment-cancel.spec.ts, tests/e2e/appointment-reschedule.spec.ts, tests/e2e/patient-dashboard.spec.ts (the assertions that must keep passing unedited)
  </read_first>
  <action>
    Change `AppointmentBadge` in `lib/appointments.ts` so its first member is `labelKey: TranslationKey` in place of `label: string`, and have `appointmentBadge()` return the `appointment_status` key corresponding to each of its four existing branches instead of an English sentence. Leave `splitAppointments()` and `isCancelledStatus()` untouched, and preserve the module's header comment explaining that the derived label is presentation-only and is never written back to `appointments.status`.

    Update all three call sites in this same task, because the signature change breaks compilation until every one is migrated. Render the badge text as `t(badge.labelKey)`.

    Handle the eligibility predicate with care. `app/patient/appointments/page.tsx` decides whether to show the Reschedule and Cancel controls by comparing the badge's label against a literal English string, and `app/doctor/(gated)/appointments/page.tsx` mirrors it. Both comparisons must switch to comparing `badge?.labelKey` against the `appointment_status` confirmed key. Leaving a comparison against rendered text would make a patient's or doctor's ability to cancel an appointment depend on the interface language — a permission decision driven by display copy. Do not change the predicate's meaning: it still means "the slot has not started and the appointment is not cancelled", and `isCancelledStatus()` is still applied alongside it.

    Then translate the remaining copy on all three pages: headings, section titles, empty states, error states, both dialogs' titles, bodies, field labels and footer buttons, in-flight button labels, and the composed aria-labels on the reschedule and cancel buttons — translate the static prefix and compose the day/time remainder as a separate value.

    Do not change any fetch, any dialog open/close handler, any state variable name, the untrimmed cancel-reason round trip, or the reschedule slot-picker behaviour. Only strings and the badge key change.

    Use only logical-property utilities in any markup you touch.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/appointment-history.spec.ts tests/e2e/appointment-cancel.spec.ts tests/e2e/appointment-reschedule.spec.ts tests/e2e/patient-dashboard.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test tests/e2e/appointment-history.spec.ts tests/e2e/appointment-cancel.spec.ts tests/e2e/appointment-reschedule.spec.ts tests/e2e/patient-dashboard.spec.ts` reports 0 failed, with `git diff --stat tests/e2e/` reporting no change.
    - `lib/appointments.ts` contains `labelKey` and contains none of the four literal badge sentences it previously returned.
    - `grep -rn 'label === ' app/patient/appointments/page.tsx app/doctor/\(gated\)/appointments/page.tsx` returns no match.
    - Both appointment pages compare `labelKey` against the appointment-status confirmed key in their eligibility predicate.
    - With the locale cookie set to `he`, a confirmed upcoming appointment on `/patient/appointments` still renders both a Reschedule control and a Cancel control, proving eligibility is language-independent.
    - With the locale cookie set to `he`, `/patient/appointments`, `/doctor/appointments` and `/patient` render Hebrew headings and Hebrew status badges.
    - `git diff --stat dictionaries/` reports no change.
    - `npx tsc --noEmit` exits 0 and `npm run lint` exits 0.
  </acceptance_criteria>
  <done>The derived status label is a dictionary key everywhere, both appointment pages and the patient dashboard translate fully, and no permission decision depends on rendered text.</done>
  <reversibility rating="reversible">The label-to-key signature change has exactly three call sites, all migrated in this task; reverting is a mechanical inverse edit.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Favorites page, doctor dashboard, doctor schedule and change-password page</name>
  <files>app/patient/favorites/page.tsx, app/doctor/(gated)/page.tsx, app/doctor/(gated)/schedule/page.tsx, app/doctor/change-password/page.tsx</files>
  <read_first>
    - app/patient/favorites/page.tsx (built in 06-02 — page title, empty-state heading and body, the CTA, the load error, and the next-available line reused from DoctorCard)
    - app/doctor/(gated)/page.tsx (built in 06-03 — heading, both stat-card captions, both quick-link labels, and the Suspense fallback)
    - app/doctor/(gated)/schedule/page.tsx (the largest remaining surface: the slot form, the blocked-period form, every label and placeholder, the list headings, the day groupings, the delete confirmations, the error messages, and every aria-label)
    - app/doctor/change-password/page.tsx (headings, field labels, submit copy, success and failure messages)
    - dictionaries/en.json (`patient_favorites.*`, `doctor_dashboard.*`, `doctor_schedule.*`, `auth.change_password.*`)
    - lib/i18n/validation-messages.ts (`translateValidationMessage`, needed wherever these pages surface a `lib/validation/availability.ts` or `lib/validation/auth.ts` message)
    - lib/i18n/server.ts (`getT`, for the doctor dashboard Server Component) and lib/i18n/locale-provider.tsx (`useT`, for the three Client Components)
    - tests/e2e/doctor-schedule-add-slot.spec.ts, tests/e2e/doctor-schedule-block-period.spec.ts, tests/e2e/doctor-schedule-delete-slot.spec.ts, tests/e2e/doctor-schedule-overlap.spec.ts, tests/e2e/doctor-schedule-visibility.spec.ts, tests/e2e/doctor-dashboard.spec.ts (the assertions that must keep passing unedited)
  </read_first>
  <action>
    Replace every user-facing literal in the four files with a `t("<key>")` call. `app/doctor/(gated)/page.tsx` is a Server Component and awaits `getT()`; both its `Suspense` fallback and its async stats child need their own translation access. The other three are Client Components and call `useT()`.

    Wherever a form surfaces a message returned by a `lib/validation/*.ts` function — the schedule page's slot and blocked-period forms, and the change-password form — wrap it in `translateValidationMessage(message, t)` at the point it is rendered. Do not modify any validation function, any route handler, or the shape of the state that holds a message.

    On the schedule page, preserve every behaviour Phase 4 established: the overlap rejection's generic message still originates in the route by branching on the Postgres error code, the reason field still round-trips byte-identically without trimming, and the delete flow still distinguishes a booked slot from a deletable one through live server state read in the same request. Translation happens only where a message reaches the DOM.

    Translate aria-labels as well as visible text, composing any day/time remainder as a separate value.

    Leave `lib/timezone.ts` and its call sites untouched — day and time formatting stays `en-US` over `Asia/Jerusalem`, for the same reason given in 06-08: changing the formatter locale would alter strings the Phase 3, 4 and 5 specs assert on, and date-format localisation is not in scope for I18N-01 or I18N-02.

    Modify nothing under `app/admin/` or `components/admin/`. D-04 excludes admin from translation entirely, and an admin-only validation message left unmapped in 06-07 correctly passes through as English.

    Use only logical-property utilities in any markup you touch.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/doctor-schedule-add-slot.spec.ts tests/e2e/doctor-schedule-block-period.spec.ts tests/e2e/doctor-schedule-delete-slot.spec.ts tests/e2e/doctor-schedule-overlap.spec.ts tests/e2e/doctor-schedule-visibility.spec.ts tests/e2e/doctor-dashboard.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - The six spec files named in the verify command report 0 failed, with `git diff --stat tests/e2e/` reporting no change.
    - With the locale cookie set to `he`, `/patient/favorites`, `/doctor`, `/doctor/schedule` and `/doctor/change-password` each render Hebrew headings and Hebrew control labels.
    - With the locale cookie set to `he`, submitting the schedule page's slot form with invalid input renders a Hebrew validation message.
    - With the locale cookie set to `he`, an overlapping slot submission still renders the same generic overlap rejection the route returns, and the slot is still not created.
    - `git diff --name-only` lists no file under `app/admin/` or `components/admin/`.
    - `git diff --stat lib/timezone.ts` and `git diff --stat lib/validation/` report no change.
    - `git diff --stat dictionaries/` reports no change.
    - `npx playwright test` (full suite) reports 0 failed.
    - `npx tsc --noEmit` exits 0 and `npm run lint` exits 0.
  </acceptance_criteria>
  <done>Every remaining patient and doctor surface renders in either language, validation errors included, with admin untouched and every Phase 4 schedule guarantee intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dictionary value → rendered DOM on authenticated pages | Translated strings appear on every page a signed-in patient or doctor sees |
| rendered status label → permission decision | Two pages currently gate the cancel and reschedule controls on a comparison against rendered text |
| validator message → translated form error | A message string is used as a lookup identifier at the render boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-40 | Elevation of Privilege | eligibility predicate keyed on a translatable string | high | mitigate | Both appointment pages switch to comparing the stable `labelKey` identifier, so control visibility cannot vary with the interface language; the server-side `cancel_appointment()` and `reschedule_appointment()` guards remain the real authorization boundary regardless |
| T-06-41 | Tampering | XSS through a translated string on an authenticated page | medium | mitigate | Every `t()` result is rendered as a React child; no `dangerouslySetInnerHTML` is introduced and dictionary values are repo-authored |
| T-06-42 | Tampering | a retrofit silently changing a rendered English string | medium | mitigate | The existing Phase 1-5 specs are the regression detector and may not be edited; a red spec means the retrofit is wrong |
| T-06-43 | Information Disclosure | the doctor schedule retrofit reintroducing patient identity or reason text | high | mitigate | No select string, no fetch and no route is changed by this plan, so the Phase 5 CR-02 patient-email fix and the T-05-08 omissions stay exactly as shipped |
| T-06-44 | Tampering | admin surfaces drifting into scope | low | mitigate | An acceptance criterion asserts `git diff --name-only` lists no file under `app/admin/` or `components/admin/` |
| T-06-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package-manager install occurs in this plan |
</threat_model>

<verification>
- `npx playwright test` reports 0 failed with `git diff --stat tests/e2e/` reporting no change.
- `git diff --stat dictionaries/ lib/validation/ lib/timezone.ts` reports no change.
- `git diff --name-only` lists no file under `app/admin/` or `components/admin/`.
- `npm run build` succeeds and `npm run lint` exits 0.
</verification>

<success_criteria>
Every authenticated surface and every shared component renders in Hebrew or English from the shared dictionary, the derived status label is a stable key rather than a sentence, no permission decision depends on rendered text, and admin remains English-only.
</success_criteria>

<output>
Create `.planning/phases/06-dashboards-notifications-localization/06-09-SUMMARY.md` when done.
</output>
