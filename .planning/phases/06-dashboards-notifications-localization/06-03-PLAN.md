---
phase: 06-dashboards-notifications-localization
plan: 03
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - app/patient/page.tsx
  - app/doctor/(gated)/page.tsx
autonomous: true
requirements:
  - PATIENT-04
  - DOCTOR-01
  - DOCTOR-02

estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The patient home route renders a summary of the patient's next upcoming appointments instead of the 'Nothing here yet' placeholder (PATIENT-04)."
    - "The doctor home route renders a count of upcoming appointments (DOCTOR-01) and a count of remaining available slots (DOCTOR-02) instead of the 'Nothing here yet' placeholder."
    - "The doctor upcoming-appointment count uses a strict start_at > now() predicate: a slot starting exactly at the query instant is not counted, and a slot starting one second later is (DOCTOR-01 boundary edge)."
    - "An appointment whose status is cancelled_by_patient or cancelled_by_doctor is excluded from the doctor upcoming count (DOCTOR-01 boundary edge)."
    - "The available-slots count uses status = 'available' AND start_at > now(): a slot starting exactly at the query instant is excluded, and a future slot whose status is booked or blocked is excluded (DOCTOR-02 boundary edge)."
    - "not applicable: DOCTOR-01 has no precision surface — the value is an exact integer returned by Postgres count, with no averaging, rounding, or floating-point arithmetic anywhere in its path (DOCTOR-01 precision edge)."
    - "not applicable: DOCTOR-02 has no precision surface — the value is an exact integer returned by Postgres count, with no averaging, rounding, or floating-point arithmetic anywhere in its path (DOCTOR-02 precision edge)."
    - "Both dashboard counts are computed server-side from a database query and never derived from a client-supplied value or a client-side array length."
    - "A patient with zero upcoming appointments sees the dedicated empty state — heading 'No upcoming appointments', body 'Book a doctor to see your next appointment here.', and a default-variant 'Find a doctor' CTA to /search — independently of whether past appointments exist (UI-SPEC empty)."
    - "The patient dashboard shows a skeleton on its first-mount fetch, reusing /patient/appointments's existing skeleton treatment (UI-SPEC loading)."
    - "Each doctor dashboard count card renders a Skeleton sized to its 32px stat number while the count query resolves (UI-SPEC loading)."
    - "The patient dashboard renders between 1 and 3 upcoming rows in day/time + doctor-name + status-badge format with no Reschedule or Cancel buttons, plus three outline quick links labelled 'Search doctors', 'My favorites' and 'Appointment history' (UI-SPEC populated)."
    - "The doctor dashboard renders exactly two stat cards captioned 'Upcoming appointments' and 'Available slots', plus a default-variant 'Manage my schedule' CTA and an outline 'My appointments' link (UI-SPEC populated)."
    - "The patient dashboard renders 1, 2 and 3 upcoming rows with identical row markup; zero routes to the empty state rather than an empty list (UI-SPEC zero-one-many)."
    - "A doctor dashboard count of 0 renders as the number 0, never as an empty or missing-data state."
    - statement: "If the patient dashboard's appointments fetch fails it surfaces the same error treatment as /patient/appointments, and if a doctor dashboard count query fails that card does not render a fabricated number — no dedicated dashboard-level error copy was specified in the UI-SPEC, so this needs executor-pass confirmation (UI-SPEC error backstop)."
      verification: backstop
  artifacts:
    - app/patient/page.tsx
    - app/doctor/(gated)/page.tsx
  key_links:
    - "app/patient/page.tsx consumes the existing GET /api/patient/appointments endpoint and lib/appointments.ts's splitAppointments() — no new endpoint and no second upcoming/past derivation."
    - "app/doctor/(gated)/page.tsx resolves doctors.id from profile_id = auth.uid() with the same derivation requireDoctor() uses, then runs both counts through the session-bound client so RLS stays a real second layer."
    - "Both dashboards' quick links point at routes that already exist or are created in this phase: /search, /patient/favorites (06-02), /patient/appointments, /doctor/schedule, /doctor/appointments."
  prohibitions:
    - "No dashboard surface may render medical content — diagnosis, prescription, clinical notes, or a reason-for-visit — even where the underlying row carries a free-text field."
    - "A dashboard count must never be computed from a client-supplied value, a URL parameter, or a number returned in a request body."
    - "The patient dashboard must never expose Reschedule or Cancel controls — appointment management stays on /patient/appointments so the summary cannot become a second, divergent mutation surface."
    - "The doctor dashboard must never surface another doctor's counts: every count query carries the caller's own resolved doctors.id."
---

<objective>
Replace both role home-page placeholders with real dashboards: the patient sees a summary of their next upcoming appointments plus quick links, and the doctor sees a count of upcoming appointments, a count of remaining available slots, and a link into schedule management.

Purpose: PATIENT-04, DOCTOR-01 and DOCTOR-02 are the "personalized home base" half of the phase goal, and they are the only Phase 6 requirements with no new API surface — both are satisfied by composing data that already exists.
Output: two rewritten page files, no new routes, no new components, no schema change.
</objective>

<execution_context>
@C:/Users/mellu/Desktop/full_stack_final_project/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/mellu/Desktop/full_stack_final_project/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-dashboards-notifications-localization/06-CONTEXT.md
@.planning/phases/06-dashboards-notifications-localization/06-PATTERNS.md
@.planning/phases/06-dashboards-notifications-localization/06-UI-SPEC.md
</context>

## Artifacts this phase produces

Created by this plan:

- `app/patient/page.tsx` — default export renamed to `PatientDashboardPage`, plus module-local `DashboardAppointmentRow` and `UpcomingSummarySkeleton`
- `app/doctor/(gated)/page.tsx` — default export renamed to `DoctorDashboardPage`, plus module-local async `DoctorDashboardStats` and `DoctorDashboardStatsSkeleton`

No new routes, components, library modules, dictionaries, or migrations.

## Flagged assumptions

- **PATIENT-04 edge probe returned `unclassified`** (spec-less probe fallback, §A). The engine generated no specific edge question for "Patient dashboard shows a summary of upcoming appointments", so this row stays **unresolved** rather than being auto-dismissed. Planner assumption carried forward: the reachable edges are the zero/one/many row counts and the empty state already resolved from the UI-SPEC, and the upcoming/past boundary already owned and tested by `lib/appointments.ts`'s `splitAppointments()` from Phase 5. Surface this at verify time; do not treat it as covered without evidence.
- **Routing decision** (Claude's Discretion in CONTEXT.md, RESEARCH Open Question 1): the dashboards replace the existing placeholder content **in place** at `app/patient/page.tsx` and `app/doctor/(gated)/page.tsx`. No new `/patient/dashboard` or `/doctor/dashboard` route, no redirect, no proxy matcher change. TASKS.md items 6 and 7 name those paths as shorthand for "the role home base"; the UI-SPEC locks the in-place reading and this plan follows it.

<tasks>

<task type="auto">
  <name>Task 1: Patient dashboard — upcoming appointment summary and quick links (PATIENT-04)</name>
  <files>app/patient/page.tsx</files>
  <read_first>
    - app/patient/page.tsx (the current placeholder being replaced — note its `main` shell and `ps-6 pe-6` logical padding)
    - app/patient/appointments/page.tsx (the loader/skeleton/error/empty status machine to mirror, the `PatientAppointment` type it exports, its `AppointmentRow` day/time + doctor-name + Badge row format, and its `AppointmentListSkeleton`)
    - lib/appointments.ts (`splitAppointments`, `appointmentBadge`, `isCancelledStatus`, `AppointmentStatus` — the single source for the upcoming/past split and the derived badge; do not reimplement either)
    - lib/timezone.ts (`formatJerusalemDayHeading`, `formatJerusalemTime` — the only permitted display conversion)
    - app/api/patient/appointments/route.ts (the response shape `{ appointments }` this page consumes)
    - app/admin/page.tsx (page-shell spacing `ps-6 pe-6 py-8` with `<h1>` then `mt-8`)
    - .planning/phases/06-dashboards-notifications-localization/06-UI-SPEC.md § "Patient dashboard" and § Copywriting Contract
  </read_first>
  <action>
    Rewrite `app/patient/page.tsx` as a `"use client"` component named `PatientDashboardPage`, replacing the "Nothing here yet" placeholder entirely. It renders under `app/patient/layout.tsx`'s existing role guard, so the viewer is always a patient.

    Import the `PatientAppointment` type from `@/app/patient/appointments/page` rather than redefining the shape, and import `splitAppointments`, `appointmentBadge` and `isCancelledStatus` from `@/lib/appointments`. Fetch `GET /api/patient/appointments` once on mount through a `useCallback` loader plus a first-mount `useEffect`, exactly as `app/patient/appointments/page.tsx` does — no new endpoint (RESEARCH Open Question 2 resolves to reuse).

    Page shell: `<main className="flex flex-1 flex-col ps-6 pe-6 py-8">`, `<h1 className="text-2xl font-semibold">My dashboard</h1>`, then an `mt-8 flex flex-col gap-8` container holding two blocks.

    Block one, heading `<h2 className="text-lg font-semibold">Upcoming appointments</h2>`. While the fetch is in flight, render the same three-`Skeleton` block `app/patient/appointments/page.tsx` uses. On fetch failure, render `"Could not load your appointments. Please try again."` in `text-sm text-destructive` plus an `outline` Retry button that re-runs the loader — this reuses the source page's own error treatment rather than inventing dashboard-specific copy. On success, compute `splitAppointments(appointments).upcoming` and render `upcoming.slice(0, 3)` rows. Each row uses the same visual format as `AppointmentRow`: day heading plus start-end time from `lib/timezone.ts`, `with Dr. {doctor.full_name}`, and the `appointmentBadge()` Badge — but with no Reschedule button and no Cancel button. Appointment management stays on `/patient/appointments`. When `upcoming` is empty, render the empty state instead of the row list: `<h3>` `"No upcoming appointments"`, body `"Book a doctor to see your next appointment here."`, and a single `default`-variant `"Find a doctor"` Button linking to `/search`. This empty state depends only on `upcoming.length === 0` — a patient with past appointments but no future ones still sees it.

    Block two, three quick links rendered as `outline`-variant Buttons using the existing `render={<Link href=... />}` idiom already used in this file: `"Search doctors"` to `/search`, `"My favorites"` to `/patient/favorites`, `"Appointment history"` to `/patient/appointments`. All three are `outline` — the only `default`-variant control anywhere on this page is the empty-state CTA, preserving the one-prominent-action-per-screen rule.

    Do not render any free-text appointment field (`cancelled_reason`, slot `reason`) anywhere on this page. Use only logical-property utilities.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/patient-dashboard.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `/patient` as a signed-in patient renders an `<h1>` with text `My dashboard` and no text `Nothing here yet`.
    - With one future appointment seeded, the page renders the text `Upcoming appointments` and a row containing `with Dr.` and the seeded doctor's name.
    - With zero future appointments, the page renders `No upcoming appointments`, `Book a doctor to see your next appointment here.`, and a link labelled `Find a doctor` with href `/search`.
    - The page renders links labelled `Search doctors` (href `/search`), `My favorites` (href `/patient/favorites`) and `Appointment history` (href `/patient/appointments`).
    - With one future appointment seeded, the page renders zero buttons labelled `Reschedule` and zero labelled `Cancel appointment`.
    - `app/patient/page.tsx` imports `splitAppointments` from `@/lib/appointments` and contains no second implementation of the upcoming/past split.
    - `app/patient/page.tsx` contains no reference to `cancelled_reason`.
    - `npx playwright test tests/e2e/patient-dashboard.spec.ts` reports 3 passed, 0 skipped.
    - `npx tsc --noEmit` exits 0 and `npm run lint` exits 0.
  </acceptance_criteria>
  <done>The patient home route summarises the next up-to-three upcoming appointments, offers three quick links, and shows a dedicated empty state, all built on the existing appointments endpoint and split helper.</done>
</task>

<task type="auto">
  <name>Task 2: Doctor dashboard — upcoming and available-slot counts (DOCTOR-01, DOCTOR-02)</name>
  <files>app/doctor/(gated)/page.tsx</files>
  <read_first>
    - app/doctor/(gated)/page.tsx (the current placeholder being replaced — reuse its existing `Manage my schedule` / `My appointments` button pair verbatim, including the default/outline variant split)
    - app/doctor/(gated)/layout.tsx (the forced-password-change gate this page renders under; the parent app/doctor/layout.tsx already ran the auth and role check)
    - app/admin/page.tsx (the exact stat-card markup to reuse: `Card className="bg-secondary"` then `CardContent` with a `text-[32px] leading-[1.2] font-semibold` value over a `text-sm leading-[1.4] font-semibold` caption, laid out in a `grid gap-8`; and the head-only `.select("*", { count: "exact", head: true })` idiom)
    - lib/auth/require-doctor.ts (the `doctors.id` derivation from `profile_id = auth.uid()` — replicate that lookup in the page rather than calling the guard, which returns a NextResponse unusable in a page)
    - app/api/doctor/slots/route.ts lines 14-24 (the exact available-slot predicate shape this count must mirror)
    - supabase/migrations/20260803230000_initial_schema.sql lines 79-89 (the `appointments` status check constraint listing the six permitted status values)
    - .planning/phases/06-dashboards-notifications-localization/06-UI-SPEC.md § "Doctor dashboard"
  </read_first>
  <action>
    Rewrite `app/doctor/(gated)/page.tsx` as an async Server Component named `DoctorDashboardPage`, replacing the "Nothing here yet" placeholder. Keep it a Server Component so both counts are computed server-side; do not add a new API route.

    Page shell: `<main className="flex flex-1 flex-col ps-6 pe-6 py-8">`, `<h1 className="text-2xl font-semibold">My dashboard</h1>`, then an `mt-8 flex flex-col gap-8` container.

    Wrap the two count cards in `<Suspense fallback={<DoctorDashboardStatsSkeleton />}>` around an async `DoctorDashboardStats` Server Component defined in the same file. `DoctorDashboardStatsSkeleton` renders the same `grid grid-cols-2 gap-8` of two `Card className="bg-secondary"` blocks with a `Skeleton` sized to the 32px stat number in place of each value, so the count queries stream in behind a skeleton rather than blocking the heading.

    Inside `DoctorDashboardStats`: call `createClient()` from `@/lib/supabase/server`, resolve the user with `auth.getUser()`, then resolve this doctor's `doctors.id` with `.from("doctors").select("id").eq("profile_id", user.id).maybeSingle()` — the same condition `requireDoctor()` and the `is_doctor_owner()` RLS helper both use. Render nothing if either resolution fails; the parent layout guard already guarantees a doctor session, so this is a defensive branch, not a user-facing error path.

    Compute both counts in a single `Promise.all`, capturing `new Date().toISOString()` once so both predicates share one instant:

    Upcoming appointments (DOCTOR-01) — query `appointments` with `.select("id, slot:availability_slots!inner(start_at)", { count: "exact", head: true })`, `.eq("doctor_id", doctorId)`, `.not("status", "in", "(cancelled_by_patient,cancelled_by_doctor)")` and `.gt("slot.start_at", nowIso)`. The `!inner` join is what makes the embedded `start_at` filter narrow the parent count. If PostgREST rejects the embedded filter combined with `head: true`, fall back to the same query without `head` and use the returned row array's length — this is still a server-side count and satisfies the same contract; record which form was used in the SUMMARY.

    Available slots (DOCTOR-02) — query `availability_slots` with `.select("*", { count: "exact", head: true })`, `.eq("doctor_id", doctorId)`, `.eq("status", "available")` and `.gt("start_at", nowIso)`. This is the exact predicate `GET /api/doctor/slots` already uses, requesting a count instead of rows.

    Both use `.gt`, never `.gte`: a slot starting at exactly the query instant is not upcoming. Render each count as `count ?? 0` so a null count from a driver error renders 0 rather than an empty card, and 0 renders as the literal number 0 — a zero count is a normal value, not a missing-data state, matching the admin dashboard's identical stat cards. Captions are `"Upcoming appointments"` and `"Available slots"`.

    Below the Suspense block, render the existing quick-link row unchanged: `"Manage my schedule"` as the one `default`-variant Button to `/doctor/schedule`, and `"My appointments"` as an `outline` Button to `/doctor/appointments`.

    Do not render any patient name, appointment reason, or slot reason on this page. Use only logical-property utilities.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/doctor-dashboard.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `/doctor` as a signed-in doctor renders an `<h1>` with text `My dashboard` and no text `Nothing here yet`.
    - The page renders the captions `Upcoming appointments` and `Available slots`, each adjacent to a numeric value.
    - With one future non-cancelled appointment and one future available slot seeded for that doctor, both values read `1`.
    - Cancelling that appointment reduces the upcoming value to `0` on the next load, and the released slot raises the available value to `2`.
    - Seeding a past available slot leaves the available value unchanged.
    - A second doctor's seeded appointments and slots do not change the first doctor's values.
    - `app/doctor/(gated)/page.tsx` contains `count: "exact"` and `.eq("status", "available")` and `.gt(`, and contains no `.gte(`.
    - `app/doctor/(gated)/page.tsx` contains `Manage my schedule` and `My appointments` with the original default/outline variant split.
    - `npx playwright test tests/e2e/doctor-dashboard.spec.ts` reports 4 passed, 0 skipped.
    - `npx playwright test` (full suite) reports 0 failed.
    - `npx tsc --noEmit` exits 0 and `npm run lint` exits 0.
  </acceptance_criteria>
  <done>The doctor home route shows two server-computed counts scoped to the signed-in doctor, streams them behind a skeleton, and keeps the existing schedule-management CTA pair.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser session → dashboard Server Component | The doctor id used for both counts must be derived from the session, never from a URL or prop |
| dashboard render → appointment data | Appointment rows carry free-text fields authored by the other party |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-10 | Information Disclosure | `app/doctor/(gated)/page.tsx` count queries | high | mitigate | `doctorId` is resolved inside the page from `doctors.profile_id = auth.uid()`, and both counts carry `.eq("doctor_id", doctorId)` through the session-bound client so `availability_slots_select_*` and `appointments_select_own_or_admin` RLS remain a real second layer |
| T-06-11 | Information Disclosure | patient dashboard row rendering | medium | mitigate | The summary renders only day/time, doctor name and derived badge; `cancelled_reason` and slot `reason` are never referenced, so free text authored by the other party cannot reach the dashboard |
| T-06-12 | Tampering | dashboard counts | medium | mitigate | Counts come from `count: "exact"` server-side queries; no count is read from a query string, request body, or client-side array length |
| T-06-13 | Information Disclosure | doctor dashboard exposure of patient identity | medium | mitigate | The doctor dashboard renders integers only — no patient name, email or appointment detail — so it cannot regress the CR-02 patient-email exposure fix from Phase 5 |
| T-06-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package-manager install occurs in this plan; `Suspense`, `Card`, `Skeleton` and `Button` are all already available |
</threat_model>

<verification>
- `npx playwright test tests/e2e/patient-dashboard.spec.ts tests/e2e/doctor-dashboard.spec.ts` reports 7 passed, 0 skipped.
- `npx playwright test` reports 0 failed.
- `npm run build` succeeds.
- `npm run lint` exits 0.
</verification>

<success_criteria>
PATIENT-04, DOCTOR-01 and DOCTOR-02 are satisfied by two rewritten pages that reuse the project's existing appointments endpoint, split helper, count-query idiom and stat-card markup, with both counts scoped to the caller and no medical content rendered anywhere.
</success_criteria>

<output>
Create `.planning/phases/06-dashboards-notifications-localization/06-03-SUMMARY.md` when done.
</output>
