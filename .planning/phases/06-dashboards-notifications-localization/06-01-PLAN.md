---
phase: 06-dashboards-notifications-localization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e/helpers/favorites.ts
  - tests/e2e/helpers/notifications.ts
  - tests/e2e/patient-favorites.spec.ts
  - tests/e2e/patient-dashboard.spec.ts
  - tests/e2e/doctor-dashboard.spec.ts
  - tests/e2e/notifications-realtime.spec.ts
  - tests/e2e/locale-switching.spec.ts
autonomous: true
requirements:
  - PATIENT-01
  - PATIENT-02
  - PATIENT-03
  - PATIENT-04
  - DOCTOR-01
  - DOCTOR-02
  - NOTIF-01
  - NOTIF-02
  - NOTIF-03
  - NOTIF-04
  - I18N-01
  - I18N-02

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "All five Phase 6 spec files exist under tests/e2e/ and are discovered by `npx playwright test --list`."
    - "Every Phase 6 behavioural test is declared as a placeholder, so `npx playwright test` reports 0 failed while Phase 6 production code does not yet exist."
    - "tests/e2e/helpers/favorites.ts and tests/e2e/helpers/notifications.ts create rows through testAdminClient() (service role) and expose a cleanup function that swallows individual row failures, matching tests/e2e/helpers/appointments.ts."
    - "Each placeholder test body contains the real assertions its owning plan must make pass — un-marking the placeholder is the only edit an implementing plan needs to activate it."
  artifacts:
    - tests/e2e/helpers/favorites.ts
    - tests/e2e/helpers/notifications.ts
    - tests/e2e/patient-favorites.spec.ts
    - tests/e2e/patient-dashboard.spec.ts
    - tests/e2e/doctor-dashboard.spec.ts
    - tests/e2e/notifications-realtime.spec.ts
    - tests/e2e/locale-switching.spec.ts
  key_links:
    - "Each new spec imports createTestUser/TEST_PASSWORD from tests/e2e/helpers/test-users.ts and the new favorites/notifications fixture helpers, never building its own service-role client inline."
    - "tests/e2e/helpers/notifications.ts is the only place a notification row is written by a test — book_appointment()/cancel_appointment()/reschedule_appointment() remain the production write path."
  prohibitions:
    - "A fixture helper must never leave rows behind: every helper that inserts must register the row id for its own cleanup function."
    - "A test must never assert on the stored notifications.message column — that column is never rendered (D-03) and asserting on it would lock in the wrong contract."
    - "Test helpers must never read the service-role key from anywhere but testAdminClient(), and must never log it."
---

<objective>
Create the Nyquist Wave 0 validation scaffolding for Phase 6: two service-role fixture helpers and five Playwright spec files covering all twelve phase requirements, authored with real assertions but declared as placeholders so the existing 290-test suite stays green until each owning plan activates its spec.

Purpose: every downstream Phase 6 task needs a runnable `<automated>` verify command. Without this plan, every task in waves 2-6 would carry `MISSING`.
Output: 7 new test files; zero production-code changes.
</objective>

<execution_context>
@C:/Users/mellu/Desktop/full_stack_final_project/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/mellu/Desktop/full_stack_final_project/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-dashboards-notifications-localization/06-VALIDATION.md
@.planning/phases/06-dashboards-notifications-localization/06-UI-SPEC.md
@tests/e2e/helpers/appointments.ts
@tests/e2e/helpers/test-users.ts
@tests/e2e/helpers/supabase-admin.ts
@playwright.config.ts
</context>

## Source Coverage Audit (phase-wide, all four sources)

Every source item is mapped to an owning plan. No item is deferred or reduced.

### GOAL (ROADMAP Phase 6 success criteria)

| # | Goal item | Owning plan(s) | Status |
|---|-----------|----------------|--------|
| G-1 | Patient can add/remove a favorite and view the full list | 06-02 | COVERED |
| G-2 | Patient dashboard shows upcoming summary; doctor dashboard shows both counts | 06-03 | COVERED |
| G-3 | Patient/doctor receive an in-app notification on booking, cancel, reschedule | 06-04, 06-06 | COVERED |
| G-4 | User can switch Hebrew/English from any page; layout mirrors to RTL | 06-05, 06-07, 06-08, 06-09, 06-10 | COVERED |

### REQ (REQUIREMENTS.md phase_req_ids)

| ID | Owning plan(s) | Status |
|----|----------------|--------|
| PATIENT-01 | 06-02 | COVERED |
| PATIENT-02 | 06-02 | COVERED |
| PATIENT-03 | 06-02 | COVERED |
| PATIENT-04 | 06-03 | COVERED |
| DOCTOR-01 | 06-03 | COVERED |
| DOCTOR-02 | 06-03 | COVERED |
| NOTIF-01 | 06-04, 06-06 | COVERED |
| NOTIF-02 | 06-04, 06-06 | COVERED |
| NOTIF-03 | 06-04, 06-06 | COVERED |
| NOTIF-04 | 06-04, 06-06 | COVERED |
| I18N-01 | 06-05, 06-07, 06-08, 06-09 | COVERED |
| I18N-02 | 06-05, 06-10 | COVERED |

### RESEARCH (06-RESEARCH.md features, patterns, pitfalls, open questions)

| Item | Owning plan | Status |
|------|-------------|--------|
| Pattern 1 head-only count query | 06-03 | COVERED |
| Pattern 2 notification type + viewer role key resolution (Pitfall 1) | 06-04 | COVERED |
| Pattern 3 Realtime publication migration | 06-04 | COVERED |
| Pattern 3 client subscription hook + cleanup | 06-06 | COVERED |
| Pattern 4 locale cookie read / Route Handler write / router.refresh() | 06-05 | COVERED |
| Pitfall 3 whole-app dynamic rendering (accepted tradeoff) | 06-05 | COVERED (recorded, no workaround) |
| Pitfall 4 validation error strings are literals, not codes | 06-07 (option a: message → key lookup) | COVERED |
| Pitfall 5 no popover component installed | 06-06 (`npx shadcn add popover`, per UI-SPEC) | COVERED |
| Pitfall 6 duplicated LANGUAGE_LABELS maps | 06-08 | COVERED |
| Pitfall 7 RTL never runtime-tested before this phase | 06-10 | COVERED |
| Open Question 1 dashboard routing | 06-03 (resolved: replace placeholders in place, per UI-SPEC) | COVERED |
| Open Question 2 dashboard fetch shape | 06-03 (resolved: reuse GET /api/patient/appointments) | COVERED |
| Assumption A1 realtime token auto-refresh | 06-06 (flagged assumption) | COVERED |
| Assumption A2 router.refresh() re-renders root layout | 06-05 (flagged assumption) | COVERED |
| Assumption A3 message-string dictionary lookup | 06-07 (locked as option a) | COVERED |
| Package Legitimacy Audit: zero new packages | all plans (no package-manager install task exists) | COVERED |

### CONTEXT (06-CONTEXT.md D-01 … D-06)

| ID | Decision | Owning plan | Status |
|----|----------|-------------|--------|
| D-01 | Favorite toggle in two places (profile + search card) | 06-02 | COVERED |
| D-02 | Supabase Realtime postgres_changes on notifications | 06-04 (publication), 06-06 (subscription) | COVERED |
| D-03 | Notification copy from `type` + viewer role, never stored `message` | 06-04 (resolver), 06-06 (render) | COVERED |
| D-04 | i18n covers patient + doctor + public pages; admin excluded | 06-07, 06-08, 06-09 | COVERED |
| D-05 | Locale in a cookie, read server-side in root layout | 06-05 | COVERED |
| D-06 | SiteHeader always rendered, incl. logged-out | 06-05 | COVERED |

Deferred Ideas: none declared in CONTEXT.md. No item is MISSING.

## Artifacts this phase produces

Full phase-wide inventory (each item is created by the plan named in brackets):

**Route handlers** — `app/api/patient/favorites/route.ts` (`GET`, `POST`) [06-02] · `app/api/patient/favorites/[id]/route.ts` (`DELETE`) [06-02] · `app/api/notifications/route.ts` (`GET`) [06-04] · `app/api/notifications/[id]/read/route.ts` (`PATCH`) [06-04] · `app/api/locale/route.ts` (`POST`) [06-05]

**React components** — `components/favorite-toggle.tsx` (`FavoriteToggle`) [06-02] · `components/notification-bell.tsx` (`NotificationBell`, `useNotificationRealtime`) [06-06] · `components/language-switcher.tsx` (`LanguageSwitcher`) [06-05] · `components/ui/popover.tsx` (`Popover`, `PopoverTrigger`, `PopoverContent`) [06-06]

**Pages** — `app/patient/favorites/page.tsx` (`PatientFavoritesPage`) [06-02] · `app/patient/page.tsx` rewritten to `PatientDashboardPage` [06-03] · `app/doctor/(gated)/page.tsx` rewritten to `DoctorDashboardPage` + `DoctorDashboardStats` + `DoctorDashboardStatsSkeleton` [06-03]

**Layouts / global** — `app/layout.tsx` gains the locale cookie read, `<html lang>`/`<html dir>`, `<LocaleProvider>` and the single `<SiteHeader />` mount [06-05] · `app/patient/layout.tsx`, `app/doctor/layout.tsx`, `app/admin/layout.tsx` lose their own `<SiteHeader />` [06-05] · `components/site-header.tsx` loses the anonymous early return and gains the switcher [06-05] and the bell [06-06]

**Library modules** — `lib/i18n/dictionaries.ts` (`SUPPORTED_LOCALES`, `type Locale`, `type TranslationKey`, `isLocale`, `readLocale`, `translate`) [06-05] · `lib/i18n/locale-provider.tsx` (`LocaleProvider`, `useLocale`, `useT`) [06-05] · `lib/i18n/notification-copy.ts` (`type NotificationType`, `NOTIFICATION_COPY_KEYS`, `notificationCopyKey`) [06-04] · `lib/i18n/validation-messages.ts` (`VALIDATION_MESSAGE_KEYS`, `translateValidationMessage`) [06-07] · `lib/validation/favorites.ts` (`validateFavoriteInput`) [06-02] · `lib/validation/locale.ts` (`validateLocaleInput`) [06-05]

**Data files** — `dictionaries/en.json`, `dictionaries/he.json` [created 06-05, extended 06-07]

**Migration** — `supabase/migrations/20260812090000_enable_notifications_realtime.sql` [06-04]

**Tests** — the 7 files this plan creates.

<tasks>

<task type="auto">
  <name>Task 1: Service-role fixture helpers for favorites and notifications</name>
  <files>tests/e2e/helpers/favorites.ts, tests/e2e/helpers/notifications.ts</files>
  <read_first>
    - tests/e2e/helpers/appointments.ts (the exact module-level `createdIds` array + `splice`-based cleanup idiom to copy)
    - tests/e2e/helpers/supabase-admin.ts (`testAdminClient()` — the only permitted service-role client factory)
    - tests/e2e/helpers/test-users.ts (`createTestUser`, `TEST_PASSWORD`, swallow-individual-failures cleanup convention)
    - supabase/migrations/20260803230000_initial_schema.sql lines 91-107 (exact `favorites` and `notifications` column lists and the `unique (patient_id, doctor_id)` constraint)
  </read_first>
  <action>
    Create `tests/e2e/helpers/favorites.ts` exporting `createTestFavorite({ patientId, doctorId })` which inserts into `public.favorites` through `testAdminClient()`, selects `id`, pushes it onto a module-level `createdFavoriteIds` array and returns `{ id }`; `readFavoriteIds(patientId)` returning the `doctor_id` values currently stored for that patient (used to assert a POST/DELETE actually landed); and `cleanupTestFavorites()` which splices the array and deletes each id inside its own try/catch so one dead id cannot abort the rest.

    Create `tests/e2e/helpers/notifications.ts` exporting `insertTestNotification({ userId, type, message, relatedAppointmentId })` (defaults: `type` `"appointment_booked"`, `message` `"fixture row"`, `relatedAppointmentId` `null`) which inserts into `public.notifications` through `testAdminClient()` and tracks the returned id; `readNotificationsFor(userId)` returning `id, type, read_at, created_at` ordered `created_at` descending; and `cleanupTestNotifications()` with the identical splice-and-swallow shape.

    Both modules import only from `./supabase-admin`. Neither module may construct a Supabase client directly, read `process.env.SUPABASE_SERVICE_ROLE_KEY`, or print any key material. `notifications` has no insert RLS policy, so the service-role client is the only way a test can seed a row — this is deliberate and mirrors production, where only the SECURITY DEFINER appointment functions insert.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `tests/e2e/helpers/favorites.ts` exports `createTestFavorite`, `readFavoriteIds`, and `cleanupTestFavorites`.
    - `tests/e2e/helpers/notifications.ts` exports `insertTestNotification`, `readNotificationsFor`, and `cleanupTestNotifications`.
    - Both files import `testAdminClient` from `./supabase-admin` and contain no other Supabase client construction.
    - `grep -c "SUPABASE_SERVICE_ROLE_KEY" tests/e2e/helpers/favorites.ts tests/e2e/helpers/notifications.ts` reports 0 for both files.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Both fixture helpers compile, expose insert + read + cleanup, and route all privileged access through `testAdminClient()`.</done>
</task>

<task type="auto">
  <name>Task 2: Five placeholder-marked Phase 6 spec files carrying real assertions</name>
  <files>tests/e2e/patient-favorites.spec.ts, tests/e2e/patient-dashboard.spec.ts, tests/e2e/doctor-dashboard.spec.ts, tests/e2e/notifications-realtime.spec.ts, tests/e2e/locale-switching.spec.ts</files>
  <read_first>
    - tests/e2e/appointment-booking.spec.ts (login-then-act flow, `test.beforeAll`/`test.afterAll` fixture lifecycle, `page.getByRole` selector style used across this suite)
    - tests/e2e/helpers/test-users.ts (`createTestUser`, `TEST_PASSWORD`, `cleanupTestUsers`)
    - tests/e2e/helpers/favorites.ts and tests/e2e/helpers/notifications.ts (created in Task 1)
    - .planning/phases/06-dashboards-notifications-localization/06-UI-SPEC.md § Copywriting Contract (the exact user-visible strings every assertion must match)
    - .planning/phases/06-dashboards-notifications-localization/06-VALIDATION.md § Wave 0 Requirements
    - playwright.config.ts (`timeout: 30000`, `workers: 1`, `baseURL` http://localhost:3000)
  </read_first>
  <action>
    Author all five spec files with complete, real assertions, declaring every behavioural test with `test.fixme(` at the declaration site so Playwright reports it as skipped rather than failing while the feature does not exist. Each owning plan later converts its own `test.fixme(` declarations to `test(`. Do not use `test.skip` (which this suite does not use) and do not author empty or `expect(true)` bodies — a placeholder with no assertion is worthless as a Wave 0 artifact.

    `tests/e2e/patient-favorites.spec.ts` (PATIENT-01/02/03, D-01): titles `"patient adds a favorite from the doctor profile page"`, `"patient adds a favorite from a search result card"`, `"favorite state added on the profile page is reflected on the search results page"`, `"patient removes a favorite from the favorites list"`, `"favorites list shows the empty state when the patient has no favorites"`, `"another patient's favorites are never returned"`. Assertions use the `aria-label` values `"Add to favorites"` / `"Remove from favorites"`, page heading `"My favorites"`, empty-state heading `"No favorites yet"`, and `readFavoriteIds()` to prove the database row actually changed. The last title drives `page.request.delete("/api/patient/favorites/<other patient's doctorId>")` and expects status 404, then re-reads the victim's favorites to prove no side effect.

    `tests/e2e/patient-dashboard.spec.ts` (PATIENT-04): titles `"patient dashboard lists the next upcoming appointment"`, `"patient dashboard shows the empty state with no upcoming appointments"`, `"patient dashboard quick links navigate to search, favorites and appointment history"`. Uses `createTestAppointment` from `tests/e2e/helpers/appointments.ts` to seed a future slot, then asserts heading `"My dashboard"`, section heading `"Upcoming appointments"`, empty-state heading `"No upcoming appointments"`, and the three link labels `"Search doctors"`, `"My favorites"`, `"Appointment history"`.

    `tests/e2e/doctor-dashboard.spec.ts` (DOCTOR-01/02): titles `"doctor dashboard shows the upcoming appointment count"`, `"doctor dashboard shows the remaining available slot count"`, `"a cancelled appointment is not counted as upcoming"`, `"a past slot is not counted as available"`. Seeds slots/appointments through the availability and appointments helpers, then asserts the caption `"Upcoming appointments"` / `"Available slots"` and the adjacent numeric value.

    `tests/e2e/notifications-realtime.spec.ts` (NOTIF-01/02/03/04): titles `"patient sees a notification after booking"`, `"doctor sees a notification after a patient books"`, `"patient sees a notification after the doctor cancels"`, `"patient sees a notification after rescheduling"`, `"the notification badge updates without a page reload"`, `"one user never receives another user's notification"`. Call `test.setTimeout(60000)` at the top of this file because the Realtime websocket handshake exceeds the 30s default under a cold dev server. The reload-free test signs a patient in, opens any page so the bell is mounted, captures the current badge text, calls `insertTestNotification()` for that patient's own id, and asserts the badge text changes with Playwright's auto-retrying `expect()` — never a manual `waitForTimeout`, never `page.reload()`. Copy assertions use the six UI-SPEC strings, e.g. `"Your appointment has been booked."` for the patient view and `"A patient booked an appointment with you."` for the doctor view.

    `tests/e2e/locale-switching.spec.ts` (I18N-01/02): titles `"switching to Hebrew sets lang and dir on the html element"`, `"switching back to English restores ltr"`, `"the chosen locale survives a fresh navigation with no wrong-direction flash"`, `"the language switcher is available to a logged-out visitor"`, `"an invalid locale value is rejected"`. Assertions read `page.locator("html")` attributes `lang` and `dir` (expected `he`/`rtl` then `en`/`ltr`), click the switcher by its `aria-label` `"עברית"` / `"English"`, and drive `page.request.post("/api/locale", { data: { locale: "fr" } })` expecting status 400. The no-flash test seeds the cookie via `context.addCookies` and asserts `dir` is already `rtl` on the very first paint of a fresh `page.goto("/search")`.

    Register `cleanupTestFavorites`, `cleanupTestNotifications`, `cleanupTestAppointments` and `cleanupTestUsers` in each file's `test.afterAll`, in that dependency order (favorites and notifications before appointments, appointments before users), matching the on-delete-restrict ordering already documented in `tests/e2e/helpers/appointments.ts`.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/patient-favorites.spec.ts tests/e2e/patient-dashboard.spec.ts tests/e2e/doctor-dashboard.spec.ts tests/e2e/notifications-realtime.spec.ts tests/e2e/locale-switching.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test --list` includes all 27 titles enumerated in the action above.
    - `npx playwright test tests/e2e/patient-favorites.spec.ts tests/e2e/patient-dashboard.spec.ts tests/e2e/doctor-dashboard.spec.ts tests/e2e/notifications-realtime.spec.ts tests/e2e/locale-switching.spec.ts` reports 0 failed (all tests reported skipped).
    - `npx playwright test` (full suite) reports 0 failed and the same passed count as before this plan.
    - `grep -c "waitForTimeout" tests/e2e/notifications-realtime.spec.ts` reports 0.
    - `tests/e2e/notifications-realtime.spec.ts` contains `test.setTimeout(60000)`.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>All five spec files are discovered by Playwright, every behavioural test carries its real assertions in a placeholder-marked declaration, and the full suite is still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test runner → Supabase service role | Fixture helpers hold a key that bypasses every RLS policy in the project |
| repository → git history | A leaked service-role key committed in a test file is unrecoverable |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Information Disclosure | tests/e2e/helpers/favorites.ts, tests/e2e/helpers/notifications.ts | high | mitigate | Both helpers obtain privileged access only via `testAdminClient()`, which reads `SUPABASE_SERVICE_ROLE_KEY` from the environment; neither helper reads the env var itself nor logs it (asserted by a grep acceptance criterion) |
| T-06-02 | Tampering | fixture rows left in the shared remote dev database | medium | mitigate | Every helper registers created ids and exposes a cleanup function wired into each spec's `test.afterAll`, in on-delete-restrict order |
| T-06-03 | Repudiation | notification fixture rows indistinguishable from production rows | low | accept | This is a demo project on a shared dev database; STATE.md already records accumulated test residue as a known, accepted condition to be cleared before the defence |
| T-06-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package-manager install occurs in this plan or anywhere in Phase 6 — 06-RESEARCH.md § Package Legitimacy Audit records zero new external packages, and `npx shadcn add popover` (06-06) generates a source file from the already-installed `@base-ui/react` without touching package.json |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0.
- `npx playwright test` reports 0 failed; the five new spec files contribute only skipped tests.
- `npx playwright test --list | grep -c "spec.ts"` shows 33 spec files (28 existing + 5 new).
</verification>

<success_criteria>
Every Phase 6 requirement has a named, discoverable Playwright test that an owning plan can activate with a single-token edit, and the existing suite is unchanged and green.
</success_criteria>

<output>
Create `.planning/phases/06-dashboards-notifications-localization/06-01-SUMMARY.md` when done.
</output>
