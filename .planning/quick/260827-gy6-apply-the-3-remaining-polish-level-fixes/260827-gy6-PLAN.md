---
phase: quick-260827-gy6
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/doctor/(gated)/page.tsx
  - app/patient/favorites/page.tsx
  - components/notification-bell.tsx
  - app/patient/page.tsx
  - app/patient/appointments/page.tsx
  - app/doctor/(gated)/appointments/page.tsx
  - components/site-header.tsx
  - tests/e2e/visual-polish.spec.ts
autonomous: true
requirements: [GY6-01, GY6-02, GY6-03]

estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Doctor-dashboard stat cards render with an elevation shadow layered on top of the existing accent bar + tint, and the loading skeleton carries the BYTE-IDENTICAL class string as the real card, so the loaded state is still not a visual jump (the property 06-UI-REVIEW.md Pillar 6 explicitly credits)."
    - "Favorites rows render with the same elevation shadow, visually lifting each row off the page background instead of reading as a flat outline."
    - "Every dense appointment/notification LIST CONTAINER enumerated in Task 2 separates its children at 12px instead of 8px, and the loading skeleton for each of those same lists matches the loaded spacing."
    - "The site header paints a real CSS linear-gradient at runtime — its computed background-image is a linear-gradient(...) string, never the literal none — proving the Tailwind gradient utility actually generated a rule in this Tailwind v4 build."
    - "The site header's computed background-color is fully transparent, proving the flat solid fill was REPLACED rather than layered under the gradient (a fade toward a translucent stop over an opaque identical base resolves back to flat and produces no visible effect at all)."
    - "The header gradient's computed background-image is byte-identical under dir=ltr and under dir=rtl — the empirical, recorded proof that CSS gradients do NOT auto-mirror, which is the reason the chosen axis is vertical (top-to-bottom), an axis with no RTL ambiguity whatsoever."
    - "Zero component structure or logic changes: no state, fetch, route, prop, conditional-render, dictionary-key, or accessible-name change in any touched file. The production diff is Tailwind class strings only."
    - "Zero new npm packages: package.json and package-lock.json are unchanged."
    - "Zero physical-direction Tailwind utilities introduced anywhere in app/ or components/ (excluding components/ui/) — the five-phase RTL invariant still greps clean."
    - "The full Playwright suite shows no NEW failures relative to the tracked shared-dev-DB / Supabase-under-load flakiness baseline (WINDOWS.md ids 1/3/5/7/8/9/10/11/12/16); any failure touching a surface this task styled is re-run in isolation before it is classified as pre-existing."
  artifacts:
    - "app/doctor/(gated)/page.tsx — elevation utility appended to BOTH stat-card class strings (skeleton block and real block), byte-identical"
    - "app/patient/favorites/page.tsx — elevation utility on the FavoriteRow Card"
    - "components/notification-bell.tsx — 12px separation on the notification row list and its loading skeleton"
    - "app/patient/page.tsx — 12px separation on the upcoming-summary section wrapper and its skeleton"
    - "app/patient/appointments/page.tsx — 12px separation on the upcoming-section and past-section wrappers and the list skeleton"
    - "app/doctor/(gated)/appointments/page.tsx — same three sites as the patient appointments page"
    - "components/site-header.tsx — vertical two-stop gradient replacing the flat solid fill on the single <header> element"
    - "tests/e2e/visual-polish.spec.ts (new) — runtime computed-style gates for the card elevation, the header gradient, the transparent header background-color, and the dir=ltr/dir=rtl gradient identity"
  key_links:
    - "components/ui/card.tsx's base class list carries `ring-1 ring-foreground/10` and NO box-shadow utility at all. The elevation utility is therefore purely additive — tailwind-merge has no shadow-group class to resolve against — and the caller className is merged AFTER the base list by cn(), so it lands deterministically with no conflict risk."
    - "Tailwind box-shadow utilities have a ZERO horizontal offset (vertical offset + blur only), so elevation is direction-neutral by construction and needs no logical-property treatment. This is why fix 1 requires none of the border-s-/ps-/end- care that quick task 260823-mn1 needed for its accent bars."
    - "Tailwind v4 renamed the gradient family: `bg-linear-to-*` is the v4-canonical name and `bg-gradient-to-*` is a retained v3 alias. Both are expected to compile in this build (tailwindcss ^4 via @tailwindcss/postcss), but if the chosen name silently generates no rule the computed background-image comes back as the literal none — which is exactly why the gradient gate reads the live browser instead of the source file, and why Task 3 carries an explicit fallback branch."
    - "CSS gradients are NOT direction-aware. Tailwind's --tw-gradient-* stops are plain CSS Custom Properties and `linear-gradient(to right, ...)` does not mirror under dir=rtl the way ps-/pe-/start-/end-/border-s- do. This is the entire reason the header uses the vertical axis. The new spec's dir-identity assertion turns that claim into recorded evidence rather than an assumption."
    - "Removing the solid background-color class (rather than layering the gradient on top of it) is load-bearing: a from-secondary → to-secondary/70 gradient composited over an opaque secondary fill resolves back to flat secondary and produces NO visible fade. The gradient must composite against the body's --background for the effect to exist at all. In light mode secondary is oklch(0.97 0 0) over a white background; in dark mode secondary is oklch(0.269 0 0) over oklch(0.145 0 0) — the fade is real and subtle in both."
    - "06-UI-REVIEW.md fix 2 names AppointmentRow and DashboardAppointmentRow as the gap-2 offenders, but ALL THREE row components already render at gap-3 today (app/patient/appointments/page.tsx AppointmentRow, app/patient/page.tsx DashboardAppointmentRow, app/doctor/(gated)/appointments/page.tsx AppointmentRow). That part of the audit is stale. The genuine remaining 8px in dense list contexts is the section/list CONTAINER gap and the loading-skeleton gaps — which is precisely what Task 2 changes. Do NOT re-edit the row components."
    - "components/notification-bell.tsx's <li className=\"flex items-start gap-2 py-1\"> is deliberately left at 8px: that gap separates a size-2 (8px) unread dot from the text it marks, and widening it to 12px visually detaches the dot from its own row. Dense-row breathing room is a BETWEEN-row concern, not a within-row one."
    - "app/doctor/(gated)/page.tsx contains TWO separate stat-card JSX blocks — DoctorDashboardStatsSkeleton and DoctorDashboardStats. Quick task 260823-mn1 deliberately kept their class strings as identical literals (not a shared constant) so a grep gate and a human diff-reader both see the treatment land twice. Styling only one produces a visible flash on every dashboard load and regresses the Pillar 6 improvement the re-audit credits."
    - "components/admin/admin-nav.tsx renders a second flat secondary-filled bar directly beneath the header on every /admin page. Gradienting the header alone leaves a subtle lightness step at that seam. It is left unchanged (scope discipline — the audit targets the site header) but must be eyeballed on /admin as a human-check line."
    - "The existing suite has a known tracked flaky baseline (WINDOWS.md ids 1/3/5/7/8/9/10/11/12/16: shared-dev-DB test residue plus Supabase-Auth-under-load). A full-suite run takes roughly 40 minutes and is expected to surface a handful of those. Each failure must be named and classified individually; no blanket dismissal."
---

<objective>
Close the three remaining polish-level items from the re-audited `.planning/phases/06-dashboards-notifications-localization/06-UI-REVIEW.md` (score 17/24, Color BLOCKER already resolved by quick task 260823-mn1, no blockers left): card elevation for depth, breathing room in dense list containers, and a subtle header gradient replacing the flat fill.

Purpose: the re-audit's two lowest pillars are Visuals (2/4 — "flat, undifferentiated card design persists… no subtle shadows to create depth perception… Header remains flat bg-secondary (no gradient)") and the spacing rhythm note under Spacing (3/4 — "gap-2's overuse is the main culprit"). All three fixes are named as Low effort / Medium impact in the audit's own Closing Recommendations. This is the last aesthetic pass before demo/grading.

Output: an elevation utility on the doctor-dashboard stat cards (both states) and favorites rows; 12px separation on ten dense list/section containers across four files; a vertical two-stop gradient on the single site header; and one new Playwright spec that proves all three at runtime via computed styles rather than by reading source CSS.

Scope guard: this is a pure visual/styling pass. No component is restructured. No state, fetch, route, prop, conditional branch, dictionary key, accessible name, or `data-testid` is touched. No npm package is added. The production diff is Tailwind class strings and nothing else.

RTL decision, made explicitly rather than assumed (see `<threat_model>` and Task 3): the header gradient uses the VERTICAL axis (top-to-bottom), not the horizontal `to right` axis the audit's recommendation line suggests. Rationale — Tailwind's `--tw-gradient-*` stops are plain CSS Custom Properties, so a `linear-gradient(to right, ...)` does NOT auto-mirror under `dir="rtl"` the way logical `ps-`/`pe-`/`start-`/`end-` utilities do. A vertical gradient sidesteps the question entirely: top-to-bottom is identical in both writing directions. Task 3 does not merely assert this — it asserts the computed `background-image` is byte-identical under `dir=ltr` and `dir=rtl`, which is simultaneously the correctness proof for the vertical choice AND the recorded empirical evidence that the horizontal variant would not have mirrored.
</objective>

<execution_context>
@C:/Users/mellu/Desktop/full_stack_final_project/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/mellu/Desktop/full_stack_final_project/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06-dashboards-notifications-localization/06-UI-REVIEW.md
@.planning/quick/260823-mn1-apply-the-top-3-fixes-from-planning-phas/260823-mn1-SUMMARY.md
@app/globals.css
@components/ui/card.tsx
@components/site-header.tsx
@tests/e2e/visual-accent.spec.ts
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Card elevation on doctor-dashboard stat cards and favorites rows, proven by a live computed box-shadow read</name>
  <files>app/doctor/(gated)/page.tsx, app/patient/favorites/page.tsx, tests/e2e/visual-polish.spec.ts</files>
  <read_first>
    - `components/ui/card.tsx` — confirm the Card base class list still contains no box-shadow utility (it carries `ring-1 ring-foreground/10` instead), so the added elevation is additive and tailwind-merge has nothing to resolve against.
    - `app/doctor/(gated)/page.tsx` lines 17-45 and 104-118 — the explanatory comment block plus the TWO stat-card JSX sites that must stay byte-identical.
    - `tests/e2e/visual-accent.spec.ts` — the in-repo idiom this task's new spec follows: read a live computed style with `locator.evaluate((el) => getComputedStyle(el)...)`, assert against it, and fail loudly with the raw string interpolated into the assertion message rather than reporting a silent false green.
  </read_first>
  <action>
Establish the tracer slice for this whole task: one styling change, wired through the real component, proven by a real browser read — the same shape Tasks 2 and 3 then follow.

Step A — doctor dashboard (GY6-01, 06-UI-REVIEW.md Closing Recommendation 1). In `app/doctor/(gated)/page.tsx` there are exactly TWO `<Card>` elements carrying the identical accent class literal established by quick task 260823-mn1: one inside `DoctorDashboardStatsSkeleton`, one inside `DoctorDashboardStats`. Append Tailwind's `shadow-sm` elevation utility to the END of BOTH class strings, producing one byte-identical literal used twice. Do not extract it to a shared constant — mn1 deliberately kept it duplicated so a grep gate and a human diff-reader both see the treatment land at both sites, and 06-UI-REVIEW.md Pillar 6 explicitly credits that duplication for eliminating the skeleton-to-loaded visual jump. Do not reorder the existing utilities in either string; append only.

Step B — the explanatory comment above `DoctorDashboardStatsSkeleton` currently describes the treatment as a border width plus an all-sides border color plus a tint. Extend that sentence by one clause noting that a subtle elevation is now layered on for depth per the 06-UI-REVIEW.md re-audit. CRITICAL: write that clause in plain prose only — it must contain NO Tailwind class literal of any kind, because Task 1's verify gate counts literal class-string occurrences in this exact file and a class name inside a comment would inflate the count and silently invalidate the gate.

Step C — favorites rows (GY6-01, same recommendation). In `app/patient/favorites/page.tsx`, `FavoriteRow` renders a `<Card className="relative">`. Append the same `shadow-sm` utility so the row reads as lifted off the page. `relative` must stay first — the `FavoriteToggle` inside is absolutely positioned against it (`absolute top-2 end-2`), so removing or reordering `relative` would fling the heart to the viewport corner. Touch nothing else in this file in this task; its `gap-2` on the avatar+name row is intentionally NOT part of Task 2's dense-row change set (it is a within-row pairing, not a between-row separation).

Step D — create `tests/e2e/visual-polish.spec.ts` with its first describe block, `GY6: card elevation`. Both stat cards and favorites rows sit behind a login, so unlike `visual-accent.spec.ts`'s public `/search` chip test this block needs a session. Read `tests/e2e/doctor-dashboard.spec.ts` and `tests/e2e/patient-favorites.spec.ts` FIRST and copy their exact existing login/fixture idioms verbatim — do not author a new fixture helper for a styling task.

Test 1: reach `/doctor` with the doctor-session idiom from `doctor-dashboard.spec.ts`, locate the first `[data-slot="card"]`, read its computed `boxShadow`, and assert the string is neither empty nor the literal `none`.

Test 2: reach `/patient/favorites` with the patient-session-plus-seeded-favorite idiom from `patient-favorites.spec.ts` (including that spec's own cleanup), locate the first `[data-slot="card"]`, and make the identical non-`none` `boxShadow` assertion.

If either fixture cannot be reused without writing new helper code, ship only the test whose surface the existing helpers already reach, and record the reduced coverage explicitly in the SUMMARY rather than expanding the scaffolding.

Both assertions must interpolate the raw computed string into the failure message, matching the loud-failure convention `visual-accent.spec.ts` established.
  </action>
  <behavior>
    - A stat card on `/doctor` reports a non-empty, non-`none` computed `box-shadow`.
    - A favorites row on `/patient/favorites` reports a non-empty, non-`none` computed `box-shadow`.
    - The skeleton and real stat-card class literals are character-for-character equal, so no visual jump occurs when the Suspense boundary resolves.
    - Every existing accessible name, `data-testid`, and role on both pages is unchanged, so no existing locator in the suite stops matching.
  </behavior>
  <verify>
    <automated>cd "C:/Users/mellu/Desktop/full_stack_final_project" && npx tsc --noEmit && npx eslint "app/doctor/(gated)/page.tsx" app/patient/favorites/page.tsx tests/e2e/visual-polish.spec.ts && test "$(grep -cF 'border-s-4 border-primary bg-primary/5 shadow-sm' 'app/doctor/(gated)/page.tsx')" = "2" && test "$(grep -cF 'className="relative shadow-sm"' app/patient/favorites/page.tsx)" = "1" && npx playwright test tests/e2e/visual-polish.spec.ts tests/e2e/doctor-dashboard.spec.ts tests/e2e/patient-favorites.spec.ts --reporter=list</automated>
    <human-check>Load `/doctor` and `/patient/favorites` in the browser. The cards should read as gently lifted off the page, not outlined boxes — and the stat cards must not visibly change shape or shift when the skeleton resolves into the real numbers.</human-check>
  </verify>
  <done>Both stat-card sites and the favorites row carry the elevation utility; the doctor-page grep gate returns exactly 2 and the favorites gate exactly 1; `tsc` and `eslint` are clean; the new elevation tests plus both existing dashboard/favorites specs pass.</done>
</task>

<task type="auto">
  <name>Task 2: 12px separation on the ten dense appointment and notification list containers</name>
  <files>components/notification-bell.tsx, app/patient/page.tsx, app/patient/appointments/page.tsx, app/doctor/(gated)/appointments/page.tsx</files>
  <read_first>
    - `components/notification-bell.tsx` around lines 233-276 — the loading-skeleton column, the error column, the row `<ul>`, and the row `<li>`. Only two of those four change.
    - `app/patient/page.tsx` lines 18-26 (`UpcomingSummarySkeleton`) and 104 (the section wrapper holding the `patient_dashboard.upcoming_heading` h2).
    - `app/patient/appointments/page.tsx` lines 71-79 (`AppointmentListSkeleton`) and 519-553 (the two section wrappers carrying `data-testid="upcoming-section"` / `data-testid="past-section"`).
    - `app/doctor/(gated)/appointments/page.tsx` lines 39-47 and 289-320 — the structurally identical skeleton and two section wrappers.
  </read_first>
  <action>
Implement 06-UI-REVIEW.md Closing Recommendation 2 (GY6-02) — but against the sites that are ACTUALLY still at 8px, not the ones the audit names.

Before editing, confirm the stale-audit finding for the record: `AppointmentRow` in `app/patient/appointments/page.tsx`, `DashboardAppointmentRow` in `app/patient/page.tsx`, and `AppointmentRow` in `app/doctor/(gated)/appointments/page.tsx` all already render their row contents at `gap-3`. The audit's Recommendation 2 is stale on that point. Do NOT re-edit those three row components. Note this explicitly in the SUMMARY so a future reader does not chase the same ghost.

Change exactly these TEN container sites from `gap-2` to `gap-3`, each anchored by a unique nearby marker rather than by line number (line numbers will shift as you edit):

1. `components/notification-bell.tsx` — the flex column rendered in the `status === "loading"` branch that wraps the two `h-12` Skeletons.
2. `components/notification-bell.tsx` — the `<ul>` that maps `rows` (the only `<ul>` in the file).
3. `app/patient/page.tsx` — the flex column inside `UpcomingSummarySkeleton` that wraps the three Skeletons.
4. `app/patient/page.tsx` — the flex column whose first child is the `patient_dashboard.upcoming_heading` h2.
5. `app/patient/appointments/page.tsx` — the flex column inside `AppointmentListSkeleton`.
6. `app/patient/appointments/page.tsx` — the element carrying `data-testid="upcoming-section"`.
7. `app/patient/appointments/page.tsx` — the element carrying `data-testid="past-section"`.
8. `app/doctor/(gated)/appointments/page.tsx` — the flex column inside `AppointmentListSkeleton`.
9. `app/doctor/(gated)/appointments/page.tsx` — the element carrying `data-testid="upcoming-section"`.
10. `app/doctor/(gated)/appointments/page.tsx` — the element carrying `data-testid="past-section"`.

Leave every other 8px gap in these four files exactly as it is. Specifically and deliberately unchanged: the notification `<li>` dot-to-text gap (it separates a `size-2` unread dot from the text it marks — widening it detaches the dot from its own row); the notification error-state centred column; the reschedule-dialog internals in `app/patient/appointments/page.tsx`; and the cancel-dialog column in `app/doctor/(gated)/appointments/page.tsx`.

The skeleton sites (1, 3, 5, 8) are included on purpose: they are the loading stand-ins for the very lists whose spacing is changing, and keeping them in sync preserves the no-visual-jump property that 06-UI-REVIEW.md Pillar 6 credits.

Add NO code comments to these four files in this task. The verify gate counts literal class occurrences per file, and a class name inside a new comment would inflate the count and invalidate the gate. This is a pure class swap and needs no explanatory prose in-file.

Do not touch any `data-testid` value, any translation key, any conditional branch, or any handler in these files.
  </action>
  <verify>
    <automated>cd "C:/Users/mellu/Desktop/full_stack_final_project" && npx tsc --noEmit && npx eslint components/notification-bell.tsx app/patient/page.tsx app/patient/appointments/page.tsx "app/doctor/(gated)/appointments/page.tsx" && test "$(grep -cE '\bgap-2\b' components/notification-bell.tsx)" = "2" && test "$(grep -cE '\bgap-3\b' components/notification-bell.tsx)" = "2" && test "$(grep -cE '\bgap-2\b' app/patient/page.tsx)" = "1" && test "$(grep -cE '\bgap-3\b' app/patient/page.tsx)" = "6" && test "$(grep -cE '\bgap-2\b' app/patient/appointments/page.tsx)" = "5" && test "$(grep -cE '\bgap-3\b' app/patient/appointments/page.tsx)" = "10" && test "$(grep -cE '\bgap-2\b' 'app/doctor/(gated)/appointments/page.tsx')" = "1" && test "$(grep -cE '\bgap-3\b' 'app/doctor/(gated)/appointments/page.tsx')" = "8" && grep -qE 'data-testid="upcoming-section"' app/patient/appointments/page.tsx && npx playwright test tests/e2e/patient-dashboard.spec.ts tests/e2e/appointment-history.spec.ts tests/e2e/appointment-cancel.spec.ts tests/e2e/notifications-realtime.spec.ts --reporter=list</automated>
    <human-check>Open `/patient`, `/patient/appointments`, `/doctor/appointments` and the notification bell popover with several rows present. Rows should read as separated but still grouped — not drifting apart. Trigger each loading state (hard reload) and confirm the skeleton spacing matches the loaded spacing.</human-check>
  </verify>
  <done>All ten containers separate at 12px; the per-file counts are exactly 2/2, 1/6, 5/10 and 1/8 as gated above (baseline before this task was 4/0, 3/4, 8/7 and 4/5, so the deltas are +2, +2, +3 and +3 conversions with no collateral edits); `tsc` and `eslint` are clean; the four dashboard/appointment/notification specs pass with every `data-testid` still resolving.</done>
</task>

<task type="auto">
  <name>Task 3: RTL-investigated vertical header gradient, then the full regression sweep</name>
  <files>components/site-header.tsx, tests/e2e/visual-polish.spec.ts</files>
  <read_first>
    - `components/site-header.tsx` line 39 — the single `<header>` element and its current flat-fill class list.
    - `app/globals.css` lines 51-118 — the `--secondary` and `--background` values in both the `:root` and `.dark` blocks, which determine whether the fade is visible at all in each mode.
    - `app/layout.tsx` — confirms `<html dir>` is server-rendered from the locale cookie and that `SiteHeader` is mounted exactly once, directly inside `<body>`, so `getByRole("banner")` resolves it unambiguously.
    - `tests/e2e/locale-switching.spec.ts` lines 35-60 — the exact in-repo idiom for flipping the interface to Hebrew (click the `עברית` button, then await `html[dir="rtl"]`). Reuse it verbatim; do not hand-set the cookie.
  </read_first>
  <action>
Implement 06-UI-REVIEW.md Closing Recommendation 3 (GY6-03) with the RTL question resolved empirically, not assumed.

Step A — investigate before applying. Start the dev server and, in a scratch check (a throwaway Playwright script under the scratchpad, or `npx playwright test` on a temporary test you delete afterwards), apply a HORIZONTAL `bg-linear-to-r from-secondary to-secondary/70` to the header temporarily and read the header's computed `background-image` first under `dir=ltr` and then, after switching the interface to Hebrew, under `dir=rtl`. Record whether the two strings differ. Expected result, which this step exists to confirm rather than presume: they are byte-identical, because Tailwind's `--tw-gradient-*` stops are plain CSS Custom Properties and CSS gradients are not writing-direction aware — a `to right` gradient keeps its physical left-to-right orientation under RTL, so it would read as fading AWAY from the reading origin in Hebrew instead of toward it. Write the observed strings into the SUMMARY as the recorded finding. Then revert the temporary horizontal class before proceeding.

Step B — apply the decided fix. Replace the header's flat solid fill with a VERTICAL two-stop gradient: `bg-linear-to-b from-secondary to-secondary/70`. Remove the solid background-color utility entirely — do not keep it alongside the gradient. That removal is load-bearing: a fade toward a 70%-alpha stop composited over an opaque fill of the SAME colour resolves back to flat and produces no visible effect whatsoever. The gradient must composite against the body's background for the effect to exist. Keep `border-b border-border`, the flex layout utilities, and the `px-4 py-3 sm:px-6` padding exactly as they are; the only change to this file is swapping one background utility for three.

Step C — guard the Tailwind v4 naming risk. `bg-linear-to-b` is the v4-canonical utility name; `bg-gradient-to-b` is the retained v3 alias. Immediately after Step B, read the header's live computed `background-image` in a browser. If it comes back as the literal `none`, the canonical name generated no rule in this build — switch to the `bg-gradient-to-b` alias (with the same `from-`/`to-` stops) and re-read. Whichever name produces a real `linear-gradient(...)` is the one that ships; record which one you used and why in the SUMMARY. Do not proceed on a `none` reading.

Step D — extend `tests/e2e/visual-polish.spec.ts` with a second describe block, `GY6: header gradient and RTL non-mirroring`, containing three tests, all locating the header via `page.getByRole("banner")` on the public `/login` page (no fixture or session needed — the header renders for anonymous visitors per D-06):
  1. The computed `background-image` contains the substring `linear-gradient` and is not the literal `none`. Interpolate the raw string into the failure message.
  2. The computed `background-color` is fully transparent (`rgba(0, 0, 0, 0)`), proving the flat solid fill was replaced rather than layered under. Interpolate the raw string into the failure message.
  3. Capture the computed `background-image` under `dir=ltr`, then switch the interface to Hebrew using the `locale-switching.spec.ts` idiom, await `html[dir="rtl"]`, re-read the header's computed `background-image`, and assert the two strings are byte-identical. The test body must carry a plain-prose comment (containing no class literal) explaining that this identity is BOTH the correctness proof for choosing the vertical axis AND the recorded evidence that a horizontal gradient would not have mirrored — so a future reader does not misread the assertion as a tautology.

Step E — closing regression sweep for the whole plan. Run the full Playwright suite unfiltered. Expect roughly 40 minutes and a handful of failures from the tracked shared-dev-DB / Supabase-under-load baseline (WINDOWS.md ids 1/3/5/7/8/9/10/11/12/16). Name and classify EVERY failure individually in the SUMMARY — no blanket dismissal. Any failure on a surface this plan styled (`/doctor`, `/patient`, `/patient/favorites`, `/patient/appointments`, `/doctor/appointments`, the notification bell, the header on any page) must be re-run in isolation and either shown green or fixed before the plan is called done. If a new failure is genuinely caused by a class change here, fix the class — do not weaken the test.
  </action>
  <verify>
    <automated>cd "C:/Users/mellu/Desktop/full_stack_final_project" && npx tsc --noEmit && npx eslint components/site-header.tsx tests/e2e/visual-polish.spec.ts && grep -qF 'from-secondary' components/site-header.tsx && grep -qF 'to-secondary/70' components/site-header.tsx && git diff --exit-code -- package.json package-lock.json && test -z "$(grep -rnE '\b(pl|pr|ml|mr)-[0-9]|\b(left|right)-[0-9]|text-left|text-right' app components --include='*.tsx' --exclude-dir=ui)" && npx playwright test tests/e2e/visual-polish.spec.ts tests/e2e/locale-switching.spec.ts tests/e2e/visual-accent.spec.ts --reporter=list && npx playwright test --reporter=list</automated>
    <human-check>View any page in English and then in Hebrew, in both a wide and a narrow viewport. The header should show a barely-perceptible top-to-bottom fade — a hint of depth, never a visible band or a colour shift. Confirm it looks identical in both languages. Then load `/admin`: the admin nav bar sits directly beneath the header and stays flat — confirm the seam between them reads as acceptable rather than as a mismatched step.</human-check>
  </verify>
  <done>The RTL investigation from Step A is performed and its observed strings are recorded in the SUMMARY; the header ships a vertical two-stop gradient with the solid fill removed; the live computed `background-image` is a real `linear-gradient(...)` and the computed `background-color` is transparent; the gradient string is byte-identical under `dir=ltr` and `dir=rtl`; `package.json` and `package-lock.json` are unchanged; the physical-direction grep returns nothing; and the full suite shows no NEW failures relative to the tracked WINDOWS.md baseline, with every failure named and classified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none newly crossed) | This plan changes Tailwind class strings and adds one read-only Playwright spec. No request parsing, no query, no route handler, no RLS policy, no auth path, and no rendered data source is touched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-gy6-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package-manager install task exists anywhere in this plan; the RESEARCH Package Legitimacy Gate is not applicable because zero packages are added. Enforced mechanically by Task 3's `git diff --exit-code -- package.json package-lock.json` gate, which fails the task if either file moves. |
| T-gy6-01 | Information Disclosure | components/site-header.tsx | low | accept | The header already resolves `profiles.full_name` and `role` for the signed-in user and renders the name. This plan changes only the container element's background utility classes; no new field is selected, no new value is rendered, and the conditional that hides the bell from admin sessions is untouched. |
| T-gy6-02 | Tampering | tests/e2e/visual-polish.spec.ts | low | mitigate | The new spec reads computed styles and clicks the public language switcher only. It must NOT import any fixture helper that holds a service-role client for the purpose of writing rows, so it cannot add residue to the shared dev database — the recurring failure class tracked as WINDOWS.md ids 1/3/5/7/8/9/10/11/12/16. Where Task 1 reuses an existing seeded-favorite fixture, it reuses that spec's own established cleanup idiom unchanged rather than authoring new privileged access. |
| T-gy6-03 | Elevation of Privilege | app/patient/appointments/page.tsx, app/doctor/(gated)/appointments/page.tsx | medium | mitigate | Both pages derive cancel/reschedule eligibility from `appointmentBadge().labelKey` (T-06-40, Phase 06 plan 06-09). This plan edits only container `gap-` utilities in those files and touches neither `lib/appointments.ts` nor any eligibility predicate; Task 2's per-file class-count gates plus `tsc` would surface any accidental edit to the surrounding logic. |
</threat_model>

<verification>
1. `npx tsc --noEmit` clean.
2. `npx eslint` clean across all seven modified files plus the new spec.
3. Per-file class-count gates for Tasks 1 and 2 return their exact expected values (2 and 1 for the elevation literals; 2/2, 1/6, 5/10, 1/8 for the spacing files).
4. `tests/e2e/visual-polish.spec.ts` passes: elevation reads non-`none`, header `background-image` is a real `linear-gradient`, header `background-color` is transparent, and the gradient string is byte-identical across `dir=ltr` and `dir=rtl`.
5. `git diff --exit-code -- package.json package-lock.json` passes — zero new dependencies.
6. The physical-direction Tailwind grep across `app/` and `components/` (excluding `components/ui/`) returns nothing, preserving the five-phase RTL invariant.
7. `git diff` of the seven production files shows Tailwind class-string changes and one prose comment clause only — no JSX structure, handler, state, prop, `data-testid`, translation key, or accessible name is altered.
8. Full Playwright suite run: every failure named and classified against the WINDOWS.md flaky baseline; any failure on a styled surface re-run in isolation and shown green.
</verification>

<success_criteria>
- 06-UI-REVIEW.md's three Closing Recommendations are all implemented: card elevation (1), 12px dense-list separation (2), and a subtle header gradient (3).
- The header gradient's RTL safety is a CHECKED result with recorded observed values, not an assumption — and the vertical axis choice is justified by that recorded evidence.
- The audit's stale claim that `AppointmentRow` / `DashboardAppointmentRow` still render at 8px is corrected in the SUMMARY, with the real remaining 8px sites named.
- Stat-card skeleton and real card remain byte-identical, preserving the no-visual-jump property.
- Zero new npm packages, zero component structure or logic changes, zero RTL regressions.
- Full Playwright suite shows no NEW failures versus the tracked baseline.
</success_criteria>

<output>
Create `.planning/quick/260827-gy6-apply-the-3-remaining-polish-level-fixes/260827-gy6-SUMMARY.md` when done.

The SUMMARY must record, at minimum:
- The Step A RTL investigation's observed computed `background-image` strings under `dir=ltr` and `dir=rtl` for the temporary horizontal gradient, and the conclusion drawn.
- Which Tailwind gradient utility name shipped (`bg-linear-to-b` or the `bg-gradient-to-b` alias) and why.
- The stale-audit finding: the three row components the audit names already rendered at 12px, and the real remaining 8px sites were the list/section containers and loading skeletons.
- The deliberate non-changes (the notification dot-to-text gap, the favorites avatar+name gap, the dialog internals, `components/admin/admin-nav.tsx`).
- Every full-suite failure, named and classified against the WINDOWS.md baseline.
</output>
