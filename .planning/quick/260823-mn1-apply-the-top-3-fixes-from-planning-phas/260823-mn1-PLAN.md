---
phase: quick-260823-mn1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/globals.css
  - .planning/phases/06-dashboards-notifications-localization/06-UI-SPEC.md
  - tests/e2e/visual-accent.spec.ts
  - app/login/page.tsx
  - app/signup/page.tsx
  - app/patient/page.tsx
  - app/doctor/(gated)/page.tsx
  - app/patient/favorites/page.tsx
  - lib/appointments.ts
  - app/patient/appointments/page.tsx
  - app/doctor/(gated)/appointments/page.tsx
  - components/search/doctor-card.tsx
autonomous: true
requirements: [MN1-01, MN1-02, MN1-03]

estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The --primary token resolves to a non-zero-chroma (genuinely hued) oklch color in BOTH the light :root block and the .dark block — the 06-UI-REVIEW.md Color pillar BLOCKER (1/4, 'only red has hue') is closed at the token level."
    - "The /login submit button renders with a hued background at runtime, not a grayscale one — proven by reading the live computed style in a browser, not by reading the CSS file."
    - "Every existing variant=default Button across the app (login submit, signup submit, patient Find-a-doctor CTA, doctor Manage-my-schedule CTA, favorites Find-a-doctor CTA, doctor-card View-profile CTA) picks up the accent with zero call-site color change."
    - "Doctor-dashboard stat cards render an accent inline-start bar plus a tinted background, and the loading skeleton renders the IDENTICAL card treatment so the loaded state is not a visual jump."
    - "Every primary CTA named in the audit carries a decorative lucide icon that is aria-hidden and leaves the button's accessible name byte-identical, so existing getByRole name-based locators keep matching."
    - "Appointment status badges carry a status-colored inline-start bar at all three call sites (patient dashboard summary, patient appointments, doctor appointments), driven from the single appointmentBadge() source."
    - "Spoken-language chips and the specialty line on doctor cards and favorites rows read as accent-colored, not neutral gray."
    - "Zero physical-direction Tailwind utilities are introduced anywhere in app/ or components/ (excluding components/ui/) — the five-phase RTL invariant still greps clean."
    - "Zero new npm packages, zero new dictionary keys (no new user-facing copy is added), zero new database work, zero component structure or logic changes beyond one additive presentation-only field on the appointmentBadge() return object."
    - "The Playwright suite shows no NEW failures relative to the tracked shared-dev-DB flakiness baseline (WINDOWS.md ids 1/3/5/7/8/10/11/12)."
  artifacts:
    - "app/globals.css — hued --primary and --ring in BOTH :root and .dark, oklch(), matching the file's existing token authoring convention"
    - "tests/e2e/visual-accent.spec.ts — new spec proving the token has chroma in both modes, the login CTA paints hued, and doctor-card chips are hued"
    - "app/login/page.tsx + app/signup/page.tsx — icon on the submit CTA"
    - "app/patient/page.tsx — icons on the Find-a-doctor CTA and the three quick links, accent class on the status badge"
    - "app/doctor/(gated)/page.tsx — accent stat cards (real + skeleton), icons on both quick links"
    - "app/patient/favorites/page.tsx — icon on the Find-a-doctor CTA, accent-tinted language chips, accent specialty line"
    - "lib/appointments.ts — additive accentClassName field on AppointmentBadge"
    - "app/patient/appointments/page.tsx + app/doctor/(gated)/appointments/page.tsx — accent class applied at the badge call site"
    - "components/search/doctor-card.tsx — accent-tinted language chips, accent specialty line, icon on the View-profile CTA"
    - ".planning/phases/06-dashboards-notifications-localization/06-UI-SPEC.md — one amendment line retiring the stale 'no brand hue exists' claim"
  key_links:
    - "components/ui/button.tsx's `default` variant is literally `bg-primary text-primary-foreground hover:bg-primary/80`. Changing one CSS variable therefore recolors every variant=default Button app-wide with no call-site edit — this is why fix #1 alone moves 30+ interactive elements and why Task 1 must land before Tasks 2/3."
    - "app/globals.css's `@layer base { * { @apply border-border outline-ring/50; } }` gives EVERY element a default border-color of --border (gray). A width-only utility such as border-s-4 with no explicit color therefore paints GRAY, not accent. Width and color classes must always be added together."
    - "cn() is clsx + tailwind-merge and a caller className is merged AFTER the cva variant classes, so bg-primary/10 deterministically beats the variant's bg-secondary, and text-primary beats text-secondary-foreground. But per-side COLOR utilities are not reliably in tailwind-merge's conflict map — always express the accent as all-sides color plus per-side width, never a per-side color utility."
    - "components/ui/badge.tsx's base class list already contains `border border-transparent`. An all-sides color class is required to defeat that transparent; a bare per-side width class alone leaves the bar invisible."
    - "lib/appointments.ts appointmentBadge() is the single source consumed by three pages, and its labelKey is load-bearing for cancel/reschedule eligibility (T-06-40, Phase 06 plan 06-09). Adding a field is safe; renaming, reordering or changing labelKey/variant is NOT and would silently break permission decisions."
    - "components/pagination-nav.tsx line 85 is the in-repo precedent for aria-hidden on a decorative lucide glyph. Every icon this task adds follows it, because the existing suite locates buttons by accessible name (for example the Log in button in tests/e2e/auth-login.spec.ts)."
    - "dictionaries/he.json is typed as Record<TranslationKey, string>, so any en.json key without a he.json twin is a tsc failure. This task adds NO copy — icons are decorative only — so neither dictionary file is touched and bilingual coverage is preserved by construction."
    - "app/doctor/(gated)/page.tsx contains TWO separate stat-card JSX blocks (DoctorDashboardStatsSkeleton and DoctorDashboardStats). Styling only one produces a visible design flash on every dashboard load."
    - "The .dark class is never applied by this app (no theme toggle exists), so the dark-mode token is only reachable in a test by adding the class to documentElement manually — that is the only way to prove the dark block is not left grayscale."
---

<objective>
Break the grayscale-only monotony flagged as a BLOCKER by 06-UI-REVIEW.md (Color pillar 1/4, overall 14/24) by applying its Top 3 Priority Fixes: a real brand accent token, deliberate accent application to primary CTAs and dashboard stat cards, and light visual texture from the already-installed lucide-react icon set plus colored chips and status-badge accents.

Purpose: the audit's finding is measurable, not aesthetic hand-waving — every token in app/globals.css except --destructive has literally `0` chroma, so the only hued pixel in the entire product is an error state. Users cannot tell a primary action from body text, dashboards read as data tables, and the app feels institutional rather than like a modern booking product. One CSS variable plus a targeted application pass fixes 30+ interactive elements.
Output: a hued accent token in both light and dark blocks, accent-treated stat cards, decorative icons on nine CTAs, accent-tinted specialty/language chips, status-colored inline-start bars on appointment badges, and one new Playwright spec proving the accent actually paints. Zero new packages, zero new copy, zero migrations, zero structural refactors.

Scope guard: this is a styling pass. The only non-CSS logic touched is one ADDITIVE presentation-only field on the object appointmentBadge() already returns. No component is restructured, no fetch/state/route logic is edited, no dictionary key is added or removed.

Deliberate documented deviation from 06-UI-SPEC.md: that contract reserves --destructive for "appointment-cancellation actions only". This task extends it by one step to the cancelled STATUS badge (a red inline-start bar only — the badge text stays neutral and the badge remains variant=outline, so it never reads as a clickable destructive affordance). This is the minimum needed to satisfy the requested "reuse existing appointment_status color semantics" and is recorded as an amendment, not an accident.
</objective>

<execution_context>
@C:/Users/mellu/Desktop/full_stack_final_project/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/mellu/Desktop/full_stack_final_project/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06-dashboards-notifications-localization/06-UI-REVIEW.md
@.planning/phases/06-dashboards-notifications-localization/06-UI-SPEC.md
@app/globals.css
@components/ui/button.tsx
@components/ui/badge.tsx
@components/ui/card.tsx
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Brand accent token in light + dark, proven end-to-end on /login</name>
  <files>app/globals.css, tests/e2e/visual-accent.spec.ts, .planning/phases/06-dashboards-notifications-localization/06-UI-SPEC.md</files>
  <read_first>
    app/globals.css (the :root block at lines 51-84 and the .dark block at lines 86-118 — note that every value except --destructive carries a literal zero chroma component)
    components/ui/button.tsx lines 10-12 (the default variant is bg-primary text-primary-foreground, which is the whole reason a single token change propagates)
    tests/e2e/auth-login.spec.ts lines 1-24 (spec authoring convention: import expect/test from @playwright/test, describe block, baseURL-relative page.goto)
    playwright.config.ts (baseURL http://localhost:3000, testDir ./tests/e2e, workers 1, webServer runs npm run dev)
  </read_first>
  <behavior>
    - Test 1 (light mode): on /login, reading --primary off document.documentElement yields an oklch() value whose chroma component is strictly greater than zero.
    - Test 2 (dark mode): after adding the dark class to document.documentElement on /login, reading --primary again yields an oklch() value whose chroma component is strictly greater than zero AND differs from the light-mode value.
    - Test 3 (end-to-end paint): the /login submit button's live computed background-color is not a grayscale color — when Chromium serializes it as rgb()/rgba() the three channels are not all equal, and when it serializes it as oklch() the chroma component is greater than zero.
    - Test 4 (accessible-name guard, baseline for Tasks 2/3): the submit control is still reachable by its accessible name so the existing suite's name-based locators are provably unaffected by anything this plan does.
  </behavior>
  <action>
    Write the failing spec first at tests/e2e/visual-accent.spec.ts, run it against the current grayscale tokens to confirm it goes RED, then change the tokens to make it GREEN.

    Spec authoring notes: give the file a single describe block named for the accent contract. Add one small module-local helper that takes a CSS color string and returns whether it is hued: if the string starts with the rgb/rgba function name, parse the three integer channels and report hued when they are not all equal; if it starts with the oklch function name, parse the second whitespace-separated component and report hued when it parses to a number greater than zero; for any other serialization, fail loudly with the raw string in the assertion message rather than silently passing. This dual handling is mandatory because Chromium's serialization of a computed color declared in oklch() is not stable across versions, and a test that only understands one form is a false-green waiting to happen. Read the custom property with getComputedStyle on document.documentElement and getPropertyValue, then trim it. For the dark case, add the dark class to document.documentElement inside page.evaluate before re-reading — the app ships no theme toggle, so this is the only way the dark block is reachable at all. For the paint assertion, resolve the submit button by its accessible name and read background-color off its computed style.

    Token change in app/globals.css, authored exactly like the surrounding declarations (same property order, same oklch() form, same two-space indent, no new custom property names, no change to the @theme inline block which already maps --color-primary and --color-ring):
    - In the :root block, set the primary token to the medical teal-blue oklch(0.52 0.105 223.128). This is Tailwind v4's own cyan-700 value, so it is guaranteed in sRGB gamut, and white text on it measures about 5.4:1, comfortably over the WCAG AA 4.5:1 floor that --primary-foreground at oklch(0.985 0 0) requires. Leave --primary-foreground untouched.
    - In the :root block, replace the grayscale ring token with oklch(0.66 0.09 223.128) so focus rings share the accent hue instead of staying neutral.
    - In the .dark block, set the primary token to oklch(0.789 0.154 211.53) — Tailwind v4's cyan-400. Dark mode inverts the shadcn convention (a light primary carrying dark --primary-foreground text), and near-black text on this value measures about 6.7:1. Leave the dark --primary-foreground untouched.
    - In the .dark block, replace the grayscale ring token with oklch(0.6 0.09 217).
    - Do NOT touch --secondary, --muted, --accent, --destructive, --border, --input, the chart tokens, or any sidebar token. The sidebar tokens in particular are dead weight in this project (no sidebar primitive exists in components/ui) and editing them would be pure noise in the diff.

    Finally, amend the Color section of 06-UI-SPEC.md: the sentence asserting that no brand hue exists in this project's tokens is now factually stale. Add a short dated amendment line directly beneath that table stating that quick task 260823-mn1 introduced a hued accent per 06-UI-REVIEW.md fix 1, that --primary is now a teal-blue in both modes, and that the accent-budget discipline in the rest of the section still stands unchanged. Do not rewrite the section — append one amendment note so the historical contract stays readable.
  </action>
  <verify>
    <automated>npx playwright test visual-accent --reporter=list</automated>
    <automated>npx tsc --noEmit</automated>
    <automated>node -e "const c=require('fs').readFileSync('app/globals.css','utf8');const s=c.split('\n.dark {');if(s.length!==2)throw new Error('could not isolate the .dark block');const g=b=>b.match(/--primary:\s*oklch\(([^)]+)\)/)[1].trim().split(/\s+/);const root=g(s[0]),dark=g(s[1]);if(Number(root[1])<=0)throw new Error('light primary still zero chroma');if(Number(dark[1])<=0)throw new Error('dark primary still zero chroma');console.log('OK light',root.join(' '),'| dark',dark.join(' '))"</automated>
  </verify>
  <done>tests/e2e/visual-accent.spec.ts exists and passes; it went RED before the token edit and GREEN after (record both runs in the summary). --primary and --ring carry non-zero chroma in BOTH the :root and .dark blocks of app/globals.css. tsc is clean. 06-UI-SPEC.md carries the amendment line.</done>
</task>

<task type="auto">
  <name>Task 2: Apply the accent deliberately — stat cards, CTA icons, favorites chips</name>
  <files>app/doctor/(gated)/page.tsx, app/patient/page.tsx, app/patient/favorites/page.tsx, app/login/page.tsx, app/signup/page.tsx</files>
  <read_first>
    app/doctor/(gated)/page.tsx lines 21-34 (DoctorDashboardStatsSkeleton) and lines 93-125 (DoctorDashboardStats cards plus the two quick-link Buttons) — the two card blocks are separate JSX and BOTH must be styled identically
    app/patient/page.tsx lines 111-148 (empty-state CTA plus the three outline quick links)
    app/patient/favorites/page.tsx lines 80-99 (specialty/location block and the language chip loop) and lines 182-191 (empty-state CTA)
    app/login/page.tsx lines 126-128 and app/signup/page.tsx lines 141-143 (the submit Buttons — both already variant=default, so they are already accent-colored after Task 1; this task only adds the glyph)
    components/pagination-nav.tsx line 85 (the aria-hidden precedent for a decorative lucide glyph)
    components/ui/button.tsx line 7 (the base class list already sizes any nested svg to size-4 and applies gap spacing, so an icon needs no sizing or spacing class of its own)
  </read_first>
  <action>
    Implements 06-UI-REVIEW.md fix 2 (accent on CTAs and stat cards) and the CTA-icon half of fix 3. No structural edits: every change is a className string or a single self-closing icon element inserted as the first child of an existing Button. Do not touch any fetch, state, handler, prop shape or dictionary.

    Icon rules that apply to every glyph added in this task and the next:
    - Import named icons from lucide-react at the top of the file, following the existing import ordering in each file.
    - Every glyph carries aria-hidden set to true and no aria-label, so the button's accessible name stays exactly the existing t() string and every name-based Playwright locator in the suite keeps matching. This is non-negotiable.
    - Never add a size or margin class to the glyph — the Button base already handles both. Never add a directional chevron or arrow glyph, because those would need mirroring under dir=rtl and this codebase does no icon mirroring.
    - Insert the glyph as the FIRST child, before the existing text expression, in every case.
    - tsc is the gate that a chosen icon actually exists in the installed lucide-react version; if any named import fails to resolve, substitute the nearest existing glyph rather than inventing one.

    Doctor dashboard stat cards (both blocks): replace the bg-secondary class on the Card with an accent treatment consisting of a four-unit inline-start border width plus an all-sides accent border color plus a five-percent accent background tint. Express it as the inline-start width utility together with the plain all-sides primary border-color utility — never a per-side border-color utility, because tailwind-merge does not reliably model those and the base-layer rule that gives every element the gray --border color would otherwise win and paint the bar gray. Card carries no border-width class of its own, so only the inline-start edge ends up visible. Apply the same class string to the skeleton Card so the loading and loaded states are pixel-identical apart from the number itself. On the real card only, add the accent text color to the 32px stat number span so the count itself reads as the focal point; leave the caption span at its current neutral color and leave the Skeleton block untouched.

    Doctor dashboard quick links: add a calendar-plus glyph to the Manage-my-schedule Button (already variant=default, now accent-filled) and a calendar-days glyph to the My-appointments outline Button.

    Patient dashboard: add a search glyph to the empty-state Find-a-doctor Button, and to the three outline quick links add a search glyph, a heart glyph and a calendar-days glyph respectively, matching each link's meaning. Leave the variant split exactly as-is — 06-UI-SPEC.md's one-prominent-action-per-screen rule still holds and this task must not promote an outline button to default.

    Favorites page: add a search glyph to the empty-state Find-a-doctor Button. Then apply the chip treatment: on each language Badge in the chip loop, keep variant=secondary and add a className of a ten-percent accent background tint plus the accent text color — passed via className these deterministically beat the variant's own background and text classes because cn merges the caller's classes last. Leave the demo-profile Badge and the no-availability Badge neutral, so the colored chips carry actual meaning (spoken language) rather than becoming decoration. Finally, in the specialty/location block, move the specialty span off the inherited muted color by giving it the accent text color plus a medium font weight; leave the neighborhood/city span muted so the two lines gain a real hierarchy.

    Login and signup: add a log-in glyph to the login submit Button and a user-plus glyph to the signup submit Button. Both buttons keep their full-width class, their disabled binding and their submitting/idle text expression untouched.

    Cross-file consistency note: components/search/doctor-card.tsx carries a byte-identical chip loop and specialty/location block, and Task 3 applies exactly the same two treatments there. Use the same class strings in both files so the two surfaces cannot drift.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>npx eslint app components lib</automated>
    <automated>npx playwright test patient-dashboard doctor-dashboard patient-favorites auth-login auth-signup auth-doctor-login button-native-semantics --reporter=list</automated>
    <automated>node -e "const fs=require('fs');const f=['app/patient/page.tsx','app/doctor/(gated)/page.tsx','app/patient/favorites/page.tsx','app/login/page.tsx','app/signup/page.tsx'];for(const p of f){const s=fs.readFileSync(p,'utf8');if(!/from \"lucide-react\"/.test(s))throw new Error('no lucide import in '+p);const glyphs=s.match(/<[A-Z][A-Za-z]*\s+aria-hidden/g)||[];if(glyphs.length===0)throw new Error('no aria-hidden glyph in '+p);}const d=fs.readFileSync('app/doctor/(gated)/page.tsx','utf8');const cards=d.match(/border-s-4/g)||[];if(cards.length<2)throw new Error('stat-card accent applied to only one of the two card blocks');console.log('OK')"</automated>
    <human-check>Load /patient and /doctor in both English and Hebrew. Confirm the stat-card accent bar sits on the inline-start edge in LTR and flips to the inline-end edge under dir=rtl, and that no icon overlaps or crowds its label in either direction.</human-check>
  </verify>
  <done>All five files carry decorative aria-hidden glyphs; both doctor-dashboard stat-card blocks (skeleton and real) carry the identical accent class string; favorites language chips and the specialty line render accent-colored; tsc and eslint are clean; the seven targeted spec files pass with zero failures.</done>
</task>

<task type="auto">
  <name>Task 3: Status-badge and doctor-card accents, then full-suite regression sweep</name>
  <files>lib/appointments.ts, app/patient/page.tsx, app/patient/appointments/page.tsx, app/doctor/(gated)/appointments/page.tsx, components/search/doctor-card.tsx, tests/e2e/visual-accent.spec.ts</files>
  <read_first>
    lib/appointments.ts lines 29-54 (the AppointmentBadge type and the four-branch appointmentBadge() derivation — note the file header already declares itself presentation-only)
    app/patient/page.tsx line 52, app/patient/appointments/page.tsx line 128, app/doctor/(gated)/appointments/page.tsx line 89 (the three byte-identical Badge call sites)
    app/patient/appointments/page.tsx lines 92-100 (the comment block explaining that labelKey drives cancel/reschedule eligibility — this is why the field may only be ADDED to, never reshaped)
    components/ui/badge.tsx line 8 (the base class list already contains an all-sides transparent border, which an accent must explicitly override) and lines 11-18 (the variant class strings this task's className must beat)
    components/search/doctor-card.tsx lines 87-106 (specialty/location block and chip loop — must end up byte-identical in treatment to what Task 2 did in app/patient/favorites/page.tsx)
  </read_first>
  <action>
    Implements the remaining half of 06-UI-REVIEW.md fix 3. Two changes plus the closing regression sweep.

    First, lib/appointments.ts. Add one ADDITIVE optional-free field named accentClassName of type string to the AppointmentBadge type, and return a value for it from all four existing branches. Do not rename, reorder or change labelKey or variant in any branch — plan 06-09 wired cancel and reschedule eligibility to compare labelKey against the confirmed key precisely so permission decisions are interface-language-independent (T-06-40), and reshaping either field would silently break that. Add a short comment above the new field recording that it is presentation-only, that it exists so the three consuming pages cannot drift on status color, and that it is deliberately expressed as all-sides border color plus per-side border width because tailwind-merge does not reliably model per-side border-color utilities.

    Branch values, all RTL-safe (inline-start, never a physical edge) and all reusing colors this project already owns rather than introducing a new palette:
    - Confirmed branch (variant default, solid accent background): a two-unit inline-start border width plus an all-sides sixty-percent-opacity primary-foreground border color, giving a light leading edge on the accent pill.
    - Past branch (variant secondary, light gray background): a two-unit inline-start border width plus an all-sides forty-percent-opacity muted-foreground border color.
    - Both cancelled branches (variant outline, white background): a two-unit inline-start border width plus an all-sides seventy-percent-opacity destructive border color. Do NOT add a destructive text color and do NOT change the variant — the badge must keep reading as a neutral status label with a red leading edge, not as a clickable destructive control. This is the documented one-step extension of 06-UI-SPEC.md's destructive reservation described in the objective.

    Then apply it at all three call sites by passing the new field to the Badge className prop alongside the existing variant prop. The three lines must end up identical to each other. Nothing else on those three pages changes.

    Second, components/search/doctor-card.tsx. Apply the exact two treatments Task 2 applied to app/patient/favorites/page.tsx, using the identical class strings: accent-tinted language chips (variant stays secondary, className adds a ten-percent accent background tint plus the accent text color), and the specialty span promoted off the inherited muted color to the accent text color plus a medium font weight while the neighborhood/city span stays muted. Leave the demo-profile Badge and the no-availability Badge neutral. Also add a stethoscope glyph as the first child of the View-profile Button, following the same aria-hidden rules as Task 2.

    Third, extend tests/e2e/visual-accent.spec.ts with one test that navigates to the public /search page, waits for the first doctor card's language chip to be visible, reads its computed color, and asserts it is hued using the same helper Task 1 wrote. This is a public unauthenticated surface, so it needs no fixture and no login, and it gives the chip treatment a real automated gate rather than a visual-only one.

    Finally run the closing regression sweep. The suite has a long-tracked shared-dev-DB residue and Supabase-rate-limit flakiness baseline (STATE.md blockers; WINDOWS.md ids 1/3/5/7/8/10/11/12) that recurs on almost every full run and touches seed-availability, appointment-reschedule afterAll cleanup, and the admin CRUD/link-account specs. Failures inside that known set are NOT this task's regressions, but every failure must be individually named and classified in the summary — never waved off in aggregate. If any failure falls outside that set, fix it before closing, since this is a styling-only pass and a genuine functional regression here would mean a class string broke a locator.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>npx eslint app components lib</automated>
    <automated>node -e "const fs=require('fs');const sites=['app/patient/page.tsx','app/patient/appointments/page.tsx','app/doctor/(gated)/appointments/page.tsx'];let n=0;for(const p of sites){if(/className=\{badge\.accentClassName\}/.test(fs.readFileSync(p,'utf8')))n++;}if(n!==3)throw new Error('accentClassName wired at '+n+'/3 badge call sites');const a=fs.readFileSync('lib/appointments.ts','utf8');const b=a.match(/accentClassName:/g)||[];if(b.length<5)throw new Error('accentClassName missing from a branch: found '+b.length+' occurrences, expected the type field plus four branches');console.log('OK 3/3 call sites,',b.length,'declarations')"</automated>
    <automated>M=$(grep -rInE "(^|[^a-z-])(pl-|pr-|ml-|mr-|text-left|text-right|left-\[|right-\[)" app components --include=*.tsx | grep -v "components/ui/"); echo "$M"; test -z "$M"</automated>
    <automated>npx playwright test visual-accent appointment-history appointment-cancel patient-dashboard doctor-dashboard patient-favorites search-filters search-sort-pagination doctor-profile locale-switching --reporter=list</automated>
    <automated>npx playwright test --reporter=list</automated>
    <human-check>Open /patient/appointments in Hebrew and confirm the status badge's colored leading edge sits on the inline-end (right) side under dir=rtl, mirroring the LTR placement rather than staying pinned to the physical left.</human-check>
  </verify>
  <done>appointmentBadge() returns accentClassName from all four branches and all three call sites pass it to Badge; doctor-card chips and specialty line match the favorites-page treatment exactly; the new /search chip test passes; the physical-direction grep audit returns zero matches outside components/ui; tsc and eslint clean; the full Playwright suite ran to completion and every failure is individually named and classified against the tracked flakiness baseline in the summary, with zero failures outside that set.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none newly crossed) | This task adds no route, no query, no user input path, and no data rendering path. Every edit is a CSS custom property value, a Tailwind class string, or a decorative SVG element. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-MN1-01 | Tampering | npm/pip/cargo installs | high | mitigate | No package install occurs. lucide-react is already a declared dependency and already imported by nine components; no new dependency is added, so no package-legitimacy gate applies. The verification sweep must confirm package.json and the lockfile are untouched in the diff. |
| T-MN1-02 | Information Disclosure | app/patient/appointments, app/doctor/(gated)/appointments status badges | low | accept | The accent bar is derived purely from appointment status, which is already rendered as visible text on the same badge. Adding a color to information the viewer can already read discloses nothing new, and RLS still scopes which rows are readable at all. |
| T-MN1-03 | Elevation of Privilege | lib/appointments.ts appointmentBadge() | medium | mitigate | labelKey drives cancel/reschedule eligibility (T-06-40). The task adds a field and explicitly forbids renaming, reordering or changing labelKey and variant; the Task 3 grep gate counts accentClassName declarations and the appointment-cancel plus appointment-history specs re-prove eligibility behaviour end-to-end. |
| T-MN1-04 | Tampering | decorative lucide glyphs on CTAs | low | mitigate | Glyphs are static named imports rendered as elements, never from user data, and dangerouslySetInnerHTML is not used anywhere in this task. Every glyph is aria-hidden so it cannot alter an accessible name that a security-relevant locator or a screen reader depends on. |
</threat_model>

<verification>
Run from the repo root with the dev server available (playwright.config.ts starts it via npm run dev and reuses an existing one):

1. `npx tsc --noEmit` — clean. This is also the gate that every lucide glyph name actually exists in the installed version and that the AppointmentBadge type change compiles at all three call sites.
2. `npx eslint app components lib` — clean.
3. `npx playwright test visual-accent --reporter=list` — the new accent spec passes in both light and simulated-dark modes.
4. `npx playwright test --reporter=list` — full existing suite. Classify every failure by name against the tracked shared-dev-DB/rate-limit baseline in STATE.md and WINDOWS.md ids 1/3/5/7/8/10/11/12. Zero failures outside that set.
5. Physical-direction RTL audit across app/ and components/ excluding components/ui/ returns zero matches (Task 3 verify block has the exact command).
6. `git diff --stat` shows package.json, package-lock.json, dictionaries/en.json, dictionaries/he.json and supabase/migrations/ all untouched.
</verification>

<success_criteria>
- 06-UI-REVIEW.md's Color pillar BLOCKER is closed: --primary carries real chroma in both the light and dark blocks, proven by a live browser read, not by inspecting the source file.
- Every variant=default Button in the product is visibly hued without any of them having been individually recolored.
- Doctor-dashboard stat cards read as focal points (accent bar, tinted background, accent number) and the skeleton matches them exactly.
- Nine CTAs carry decorative aria-hidden glyphs and every existing accessible-name-based Playwright locator still matches.
- Appointment status badges are visually distinguishable at a glance by status, driven from one shared source consumed by three pages.
- Specialty and language chips read as accent-colored on both the search card and the favorites row, with identical class strings in the two files.
- The RTL invariant holds: zero physical-direction utilities added, verified by grep.
- No new npm package, no new dictionary key, no migration, no structural or logic refactor beyond one additive presentation-only field.
- Full Playwright suite run to completion with every failure individually named and attributed to the pre-existing tracked flakiness baseline.
</success_criteria>

<output>
Create `.planning/quick/260823-mn1-apply-the-top-3-fixes-from-planning-phas/260823-mn1-SUMMARY.md` when done.

The summary must record: the exact oklch values landed in both blocks and the measured contrast rationale; the RED-then-GREEN evidence for Task 1's spec; the complete list of glyphs added per file; the full-suite pass/fail counts with each failure named and classified; and the 06-UI-SPEC.md destructive-reservation amendment, so a later reader knows the cancelled-badge red bar was a deliberate contract extension rather than drift.
</output>
